# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Proyecto

**Erasmus Verified** — directorio de grupos de WhatsApp/Telegram para estudiantes Erasmus en 36+ países y 528+ ciudades europeas. También muestra un mapa interactivo con partners (locales nocturnos, alojamientos, etc.) por ciudad.

La misma web sirve **dos marcas** desde un solo código: "Erasmus Verified" (la web completa) y "Erasmus Parties" (solo la parte de fiestas/nightlife, en `erasmusparties.org`). Qué marca se muestra se decide automáticamente según el dominio — ver [Experiencia dual](#experiencia-dual-verified--parties) más abajo.

> **Nomenclatura de marca**: no cambiar las etiquetas de los grupos de WhatsApp/Telegram — son nombres de datos, no de marca.

**Stack**: HTML + CSS + JS vanilla (sin ES Modules, todo con `<script>` clásicos y funciones/objetos globales), más:

- **Vite** (v8) como build tool — ya no se abre `index.html` directamente, se usa `npm run dev` para desarrollar y `npm run build` para generar la carpeta `dist/` que se despliega.
- **Supabase** como backend — base de datos (Postgres) + login de administrador. Todas las páginas piden los datos de ciudades y partners a Supabase; no queda ningún dato estático de países/ciudades en el código (ver sección de Backend).
- **React** 19 (`src/react/*`, vía `@vitejs/plugin-react`) — **única excepción** a "sin ES Modules": son islas de React dentro de HTML/scripts clásicos, no una migración completa. Islas hoy: el menú compartido de las 13 páginas públicas (ver [Navegación](#navegación)), el footer compartido de las mismas 13 páginas (ver [Footer](#footer-footerjsx)), el bottom-nav móvil (ver [Bottom-nav](#bottom-nav-móvil-768px) dentro de Navegación) y el botón flotante de volver arriba (`BackToTop.jsx`, montado junto al bottom-nav en `mount-shell.jsx`); **la familia de cards** (`Card.jsx` y sus montadores para el home, servicios y ciudades — ver [Familia de cards](#familia-de-cards-card)) — la primera vez que React pintó contenido real de datos, no solo chrome de página —; la lista de categorías/partners del aside en `ciudad.html`/`mapa.html` (ver [Lista de partners](#lista-de-partners-partnercategorylist)); y el contenido del Sheet que se abre al pulsar un partner (ver [Detalle de partner](#detalle-de-partner-partnerdetail)) — la primera con un root de React efímero, creado y desmontado en cada apertura en vez de reutilizado toda la vida de la página; y la ficha individual de un alojamiento o un viaje (`ListingDetail.jsx`, ver [Ficha individual](#ficha-individual-listingdetail)), la primera página cuyo contenido ES la isla entera (sin script clásico de orquestación propio).

**Herramientas de desarrollo**: Prettier instalado como devDependency (`npm install` para instalar). Configuración en `.prettierrc`: 4 espacios, comillas simples, semi, `printWidth: 100`, `trailingComma: es5`. Un hook de Claude Code (`.claude/settings.json`, `PostToolUse` sobre `Write|Edit`) formatea automáticamente los archivos **`.js`, `.css` y `.html`** tras cada edición — **no** `.jsx` ni `.md`: para `src/react/*.jsx` y `CLAUDE.md` hay que pasar `npx prettier --write <archivo>` a mano. `npm run format` tiene el mismo alcance que el hook (html/css/js). Ojo: varios archivos **usan finales de línea CRLF** (los `.html`, `home.css`, `nightsSection.js`, entre otros) y algunos ya estaban sin formatear antes de que existiera el hook — al editarlos con un script, conserva los finales de línea (un `git diff` de cientos de líneas para un cambio pequeño es la señal de que se han convertido) y no reformatees un archivo entero de paso, para no ensuciar el diff.

**Comandos**:

- `npm run dev` — levanta el servidor de desarrollo de Vite (recarga en caliente)
- `npm run build` — genera la versión de producción en `dist/`
- `npm run preview` — sirve `dist/` en local para probar el build final
- `npm run format` — pasa Prettier a mano por los `.html`/`.css`/`.js` del proyecto (normalmente no hace falta, ya hay un hook)
- `npm run check:sanitization` — solo el chequeo de sanitización (Capa 2, ver [Guardarraíl de sanitización](#guardarraíl-de-sanitización-capa-1--capa-2)); ya corre dentro de `npm run build`
- **WSL / `/mnt/c`**: Vite no detecta los cambios de archivo en unidades de Windows montadas y puede servir código antiguo tras editar (síntoma real: errores que ya no existen en el código). Solución: reiniciar `npm run dev` o arrancarlo con `CHOKIDAR_USEPOLLING=true npm run dev`.

**Variables de entorno**: copiar `.env.example` a `.env.local` (este último **nunca se sube al repo**, está en `.gitignore`) y rellenar `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` y `VITE_CARTO_API_KEY` con los datos del proyecto de Supabase y de CARTO (ver [Tiles del mapa](#tiles-del-mapa)). Vite las inyecta como `window.__SUPABASE_URL__` / `window.__SUPABASE_KEY__` / `window.__CARTO_API_KEY__` en el `<head>` de cada página (ver `vite.config.js`).

**Estructura del repo** (además de `src/` y las páginas `.html` de la raíz):

- `admin/index.html` — el panel de administración (ver [Panel de administración](#panel-de-administración-admin)).
- `supabase/migrations/` — solo **parte** de las migraciones (8 archivos: seed de ciudades, lectura pública, i18n JSONB, FKs/índices, CHECK de URLs). Lo creado directamente en el dashboard — `profiles`, el trigger `handle_new_user`, `private.is_admin()`, etc. — **no** está en el repo: el estado real de la base de datos se consulta con el MCP de Supabase (o el agente `supabase-schema-guardian`), nunca se deduce de estos archivos.
- `supabase/functions/translate/` — Edge Function que traduce con DeepL (ver [Internacionalización](#internacionalización-i18n)).
- `scripts/check-sanitization.js` — el chequeo de la Capa 2 de sanitización.
- `dev/sheet-demo.html` — demo interna de `sheet.js` aislado; fuera del flujo público (no se enlaza ni entra en `vite.config.js`), `npm run dev` la sirve como cualquier archivo.
- `docs/` — notas técnicas puntuales, ver [Documentos y deuda técnica](#documentos-y-deuda-técnica).
- `.claude/` — `settings.json` (el hook de Prettier), `skills/` (`/add-city`, `/add-partner`) y `agents/` (revisores, ver [Agentes de revisión](#agentes-de-revisión-claudeagents)).
- `erasmus-verified/` — **copia antigua del proyecto con su propio `.git`, ignorada por git** (`.gitignore`). No es código vivo: no la edites ni busques ahí.
- `README.md` — **obsoleto** (describe la primera versión estática con enlaces WhatsApp/Telegram en un objeto JS). La fuente de verdad es este archivo.

## Arquitectura

Todo el JS de páginas y módulos compartidos vive ahora en `src/js/` (antes era `js/`). El CSS vive en `src/css/`. Sigue sin haber ES Modules: todo son `<script>` clásicos con funciones y objetos globales, para mantener coherencia entre archivos — salvo `src/react/` (las islas de React, ver Stack arriba, [Navegación](#navegación), [Footer](#footer-footerjsx), [Familia de cards](#familia-de-cards-card), [Lista de partners](#lista-de-partners-partnercategorylist) y [Detalle de partner](#detalle-de-partner-partnerdetail)), la única carpeta con JSX/ES Modules de verdad.

### Páginas y sus scripts

| Página                | Script                                | Propósito                                                                                                                                                                                                                                                                                      |
| --------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.html`          | `src/js/index.js`, `nightsSection.js` | Home: hero con buscador (autocomplete de ciudades y partners) y stats animadas, ticker, acordeón de ciudades (pin & scrub), "Descubre partners" (pills de categoría + grid de cards de la ciudad activa), "Trending nights" (eventos con filtros de ciudad/tema/partner/fecha), servicios, CTA |
| `ciudades-todas.html` | inline                                | Directorio completo de ciudades con filtro por texto agrupado por letra (`CityCards`, ver [Familia de cards](#familia-de-cards-card))                                                                                                                                                          |
| `ciudades.html`       | `src/js/ciudades.js`                  | Ciudades de un país (`?pais=`): hero con la foto de la primera ciudad + grid de `CityCards`                                                                                                                                                                                                    |
| `ciudad.html`         | `ciudad.js`, `cityPartners.js`        | Ciudad (`?ciudad=ID`, opcional `&partner=ID` abre ese partner): grupo de WhatsApp, buscador local, lista de partners por categoría **sin mapa** (con enlace a `mapa.html`), alojamiento y escapadas. Ver [Ciudad sin mapa](#ciudad-sin-mapa-ciudadhtml)                                        |
| `mapa.html`           | `src/js/mapa.js`                      | Mapa a pantalla completa (`?city=ID`) con lista de partners sincronizada. Ver [Mapa interactivo](#mapa-interactivo-mapahtml)                                                                                                                                                                   |
| `alojamientos.html`   | inline                                | **Todos** los alojamientos verificados, gestionados directamente por Erasmus Verified (`FeaturedListings`) — no confundir con `alojamiento.html` (singular), la ficha de UNO en concreto, ver nota de nomenclatura más abajo                                                                   |
| `alojamiento.html`    | inline                                | Ficha de **un** alojamiento en concreto (`?id=ID`), `ListingDetail` — ver [Ficha individual](#ficha-individual-listingdetail)                                                                                                                                                                  |
| `servicios.html`      | inline                                | Servicios verificados: SIM, banca, transporte (`ServiceCards`)                                                                                                                                                                                                                                 |
| `viajes.html`         | inline                                | **Todos** los viajes y excursiones, organizados directamente por Erasmus Verified (`FeaturedListings`) — no confundir con `viaje.html` (singular)                                                                                                                                              |
| `viaje.html`          | inline                                | Ficha de **un** viaje en concreto (`?id=ID`), `ListingDetail` — ver [Ficha individual](#ficha-individual-listingdetail)                                                                                                                                                                        |
| `login.html`          | `src/js/login.js`                     | Login de usuario (email + contraseña) — ver [Autenticación de usuarios](#autenticación-de-usuarios-loginhtml--registrohtml--recuperarhtml)                                                                                                                                                     |
| `registro.html`       | `src/js/registro.js`                  | Registro de usuario (email + contraseña + nombre + ciudad + apellidos/universidad/intereses opcionales) — ver sección propia                                                                                                                                                                   |
| `recuperar.html`      | `src/js/recuperar.js`                 | Recuperar contraseña (pedir enlace por email + fijar nueva contraseña al volver desde ese enlace) — ver [Autenticación de usuarios](#autenticación-de-usuarios-loginhtml--registrohtml--recuperarhtml)                                                                                         |
| `admin/index.html`    | `src/js/admin.js`                     | Panel de administración (login + ciudades, partners, eventos, reporte de clics) — ver sección propia                                                                                                                                                                                           |

Las 13 páginas públicas son todas menos `admin/index.html`. Los `<script>` inline de `alojamientos`/`servicios`/`viajes` son solo de arranque (`I18n.applyTranslations()` + `initScrollReveal()`; `alojamientos.html` además redirige a `index.html` en la experiencia Parties, donde no existe esa sección). **`ciudades-todas.html` es la excepción**: su script inline (~170 líneas) lleva el filtro por texto, la agrupación por letra y `renderGroups()`, que regenera los grupos en cada tecleo (desmontando antes los roots de `CityCards`, ver [Familia de cards](#familia-de-cards-card)). Al crear una página nueva, `docs/page-boilerplate.md` lista qué `<link>`/`<script>` necesita (tokens, tipografía, `sanitize.js`…) — ya se olvidó tres veces.

> **Nomenclatura "alojamientos/viajes" vs. "alojamiento/viaje"**: plural = la página con **todos** los elementos (`alojamientos.html`, `viajes.html`); singular = la ficha de **uno** en concreto, seleccionada desde esa página, desde el buscador o desde "más similares" de otra ficha (`alojamiento.html`, `viaje.html` — mismo criterio que `ciudad.html` singular vs. `ciudades.html`/`ciudades-todas.html` plural). `alojamiento.html` se renombró primero a `alojamientos.html` para dejar libre el nombre singular; `alojamiento.html`/`viaje.html` (la ficha individual, `ListingDetail.jsx`) son ya reales, con `?id=` (no `?alojamiento=`/`?viaje=`, para no repetir la misma ambigüedad en la URL) — ver [Ficha individual](#ficha-individual-listingdetail).

### Módulos compartidos (cargados donde se necesitan)

- `src/js/lib/supabaseClient.js` — crea `window.supabaseClient`, el cliente de Supabase que usan todos los demás scripts para hablar con la base de datos.
- `src/js/services/citiesService.js` — `fetchActiveCities()` (solo `active=true`, orden por prioridad), `fetchCityById(id)` y `fetchAllCities()` (directorio completo, activas o no) para leer ciudades desde Supabase.
- `src/js/services/partnersService.js` — `fetchPartnersByCity(cityId)` (partners + sus links, con `description`/`label` ya traducidos por `I18n.tField()`), `groupPartnersByCategory(partners)`, `fetchAllPartnersForSearch()` (índice del buscador del home; `fetchNightlifePartners(limit)` existe pero **no la usa nadie**), y `fetchUpcomingEvents({ … filtros })` (eventos del home; RLS ya limita a activos y no pasados, ver [Seguridad](#seguridad-importante)).
- `src/js/tracking.js` — `trackEvent(nombre, datos)`: imprime **todos** los eventos en consola y persiste en `cta_clicks` los de `PERSISTED_TRACKING_EVENTS`: `partner_link_click` (enlace del Sheet; `link_type` = tipo del enlace, p. ej. `WEBSITE`), `partner_directions_click` ("Cómo llegar"), `partner_card_click` (CTA de una card de partner del home), `event_ticket_click` (entradas de una card de evento) y `search_result_click` (resultado del buscador del home). **Solo se guarda si el payload trae `partnerId`**, porque `cta_clicks.partner_id` es `NOT NULL` y clave foránea a `partners`: por eso un resultado de búsqueda de tipo **ciudad** se queda solo en consola (para persistirlo haría falta cambiar el esquema). Devuelve una `Promise` (nunca rechaza); `navigateToResult()` (`index.js`) la espera hasta 400 ms antes de navegar **en la misma pestaña**, porque el navegador cancela los fetch en vuelo al descargar la página (en pestaña nueva no hace falta, y `window.open()` tiene que seguir siendo síncrono para que no lo bloquee el bloqueador de pop-ups). Un evento nuevo que quieras persistir: añádelo a ese `Set` **y** pásale `partnerId`.
- `src/js/services/authService.js` — `signUp()`/`signIn()`/`signOut()`/`getSession()`/`resetPasswordForEmail()`/`updatePassword()` sobre Supabase Auth. Cargado en las 13 páginas públicas (no solo `login.html`/`registro.html`): `AuthButton()` (`navShared.jsx`) llama a `window.getSession()`/`window.signOut()` desde el propio nav compartido para pintar el icono de cuenta en toda la web. Ver [Autenticación de usuarios](#autenticación-de-usuarios-loginhtml--registrohtml--recuperarhtml).
- `src/js/experience.js` — decide si la página se muestra como "Verified" o "Parties" según el dominio. Se carga como primer script de cada página. Ver sección [Experiencia dual](#experiencia-dual-verified--parties).
- `src/js/geocoder.js` — cliente Nominatim + caché en localStorage (`erasmus_city_coords_v1`). Solo hace falta si una ciudad no trae ya sus coordenadas guardadas en Supabase.
- `src/js/map-helpers.js` — **único archivo que conoce Leaflet** (variable global `L`); cambiar proveedor de mapas = reescribir solo este archivo. Usa `CATEGORY_META` (ver `categoryMeta.js`) para los colores de los pines.
- `src/js/categoryMeta.js` — `CATEGORY_META`: las 20 categorías de partner con su `label`, `color` e `icon`. Se extrajo de `map-helpers.js` porque `ciudad.html` lo necesita sin cargar Leaflet; en páginas que usan los dos hay que cargarlo **antes** que `map-helpers.js` (script clásico, variable global). Es una de las dos listas de categorías que deben coincidir a mano con el desplegable del admin — ver [Documentos y deuda técnica](#documentos-y-deuda-técnica).
- `src/js/cityMap.js` — `mountCityMap(containerId, { pais, ciudad, lat, lng, interactive })`; devuelve una Promise con la instancia del mapa. Primero usa las coordenadas ya guardadas en Supabase y solo llama al geocoder si faltan. Hoy **solo lo usa `mapa.html`**; `interactive: false` (mapa bloqueado con overlay "toca para interactuar") sigue soportado pero ninguna página lo pide ya.
- `src/js/mapPartners.js` — la lista de partners de **`mapa.html`** + su sincronización con los marcadores del mapa (categorías activas = pines visibles). Los partners vienen de `partnersService.js`. Las cabeceras de categoría + filas de partner las pinta `PartnerCategoryList.jsx` (React) — ver [Lista de partners](#lista-de-partners-partnercategorylist) — y el contenido del Sheet que se abre al pulsar un partner lo pinta `PartnerDetail.jsx` — ver [Detalle de partner](#detalle-de-partner-partnerdetail). `ciudad.html` **ya no lo usa**: tiene su propia versión sin mapa, `cityPartners.js`.
- `src/js/cityPartners.js` — la lista de partners de `ciudad.html` sin mapa (`mountCityPartners()`). Ver [Ciudad sin mapa](#ciudad-sin-mapa-ciudadhtml).
- `src/js/nightsSection.js` — la sección "Trending nights" del home: barra de filtros (ciudad, tema, partner, fecha) que **vuelve a consultar Supabase** en cada cambio (`fetchUpcomingEvents`) y pinta las cards de evento con `Card`; la de mayor `priority` (> 0) ocupa dos columnas (`card--featured`). Visible en ambas experiencias.
- `src/js/ui/sheet.js` — el `<dialog>` que se abre al pulsar un partner (focus trap, arrastrar para cerrar). Vanilla a propósito (su cabecera rechaza convertirse en isla de React); su contenido lo pinta `PartnerDetail.jsx`. `dev/sheet-demo.html` lo prueba aislado.
- `src/js/utils/animations.js` — `window.initScrollReveal()`: un único `IntersectionObserver` que añade `is-visible` a `.anim-fade-up`/`.anim-fade-in`/`.anim-slam` (una sola vez por elemento; con `prefers-reduced-motion` los muestra directamente). Hay que **volver a llamarla tras cada re-render de una lista dinámica** — desde React, dentro de un `useEffect`, nunca desde el script clásico (ver [Familia de cards](#familia-de-cards-card)).
- `src/js/utils/skeleton.js` — `window.Skeleton` (`block()`, `render()`, `clear()`): estados de carga con las mismas clases CSS que el contenido final (`.skeleton`, `components.css` §2.7) para que el layout no salte. Un único sistema para todas las páginas.
- `src/js/utils/geolocation.js` — `getUserLocation()` (nunca rechaza: resuelve `null` si no hay permiso/soporte) y `distanceMeters()`, para el "a 800 m" de cada partner en `ciudad.html` y `mapa.html`.
- `src/js/utils/speculationRules.js` — prefetch (Speculation Rules, `eager`) de las rutas de `BOTTOM_NAV_ROUTES` para que la view transition no se note lenta en 3G; degrada limpio donde no hay soporte (Safari, Firefox). **La lista está desfasada** respecto al bottom-nav actual (`AppShell.jsx`): aún prefetchea `index.html` y `mapa.html`, que ya no son pestañas, y no incluye `login.html` ("Perfil").
- `src/utils/i18n.js`, `src/utils/translations.js`, `src/js/langSwitcher.js` — el sistema de traducción. Ver [Internacionalización](#internacionalización-i18n).
- `src/react/Nav.jsx`, `TopbarNav.jsx`, `navShared.jsx` — el menú compartido, en React. Ver [Navegación](#navegación) para el detalle completo (qué página usa cuál, por qué existe, y una regla de arquitectura importante sobre `DOMContentLoaded` que aplica a todo lo que interactúe con estos componentes desde fuera).
- `src/react/Footer.jsx`, `mount-footer.jsx` — el footer compartido, en React. Ver [Footer](#footer-footerjsx).
- `src/react/AppShell.jsx`, `mount-shell.jsx` — el bottom-nav móvil, en React. Ver [Bottom-nav](#bottom-nav-móvil-768px), dentro de la sección [Navegación](#navegación).
- `src/react/Card.jsx`, `SummaryCardGrid.jsx`, `mount-summary-cards.jsx` — la card única de la web y el grid de partners/eventos del home, en React. Ver [Familia de cards](#familia-de-cards-card) para los layouts, los tipos de CTA, las fases de migración, dos bugs reales ya corregidos que conviene no repetir y las reglas al ampliar la familia.
- `src/react/ServiceCards.jsx`, `servicesData.js`, `mount-service-cards.jsx` · `CityCards.jsx`, `mount-city-cards.jsx` — el resto de la familia (servicios, ciudades): cada uno sustituye HTML estático o `innerHTML` por `Card`. Detalle en la tabla de [Familia de cards](#familia-de-cards-card).
- `src/react/CityPartnerList.jsx`, `mount-city-partner-list.jsx` — la lista de partners de `ciudad.html` sin mapa: solo traduce cada partner a props de `Card` (`layout="compact"`).
- `src/react/BackToTop.jsx` — botón flotante "volver arriba": aparece al bajar ~80 % de la altura de pantalla, scroll suave (instantáneo con `prefers-reduced-motion`). Se monta en `mount-shell.jsx` junto a `AppShell` (por eso está en las 13 páginas públicas; en `mapa.html` la página no hace scroll y nunca se ve). Estilos `.back-to-top` en `components.css`, texto `nav.back_to_top` (es/en) en `translations.js`, y su estado (visible/oculto) vive dentro del componente — sin `DOMContentLoaded` (ver la regla en [Navegación](#navegación)).
- `src/react/PartnerCategoryList.jsx`, `mount-partner-list.jsx` — la lista de categorías/partners del aside en `ciudad.html`/`mapa.html`, en React. Ver [Lista de partners](#lista-de-partners-partnercategorylist).
- `src/react/PartnerDetail.jsx`, `mount-partner-detail.jsx` — el contenido del Sheet (descripción + enlaces + "Cómo llegar") al abrir un partner, en React — `sheet.js` (el `<dialog>` en sí) sigue siendo vanilla a propósito, no se toca. Ver [Detalle de partner](#detalle-de-partner-partnerdetail).
- `src/react/ListingDetail.jsx`, `listingsData.js`, `mount-listing-detail.jsx` — la ficha individual de un alojamiento o un viaje (`alojamiento.html`/`viaje.html`, singular — no confundir con `alojamientos.html`/`viajes.html`, el listado de TODOS). Ver [Ficha individual](#ficha-individual-listingdetail).

## Backend (Supabase)

La base de datos y el panel de administración viven en un proyecto de Supabase (Postgres + Auth). Las ciudades y partners **no se añaden editando código** — se hace desde el panel de administración en `/admin` (ver sección siguiente).

### Tablas principales

Estado comprobado contra la base de datos real (todas con RLS activado). Los campos de texto que ve el usuario son **JSONB `{ "es": …, "en": … }`** (ver [Internacionalización](#internacionalización-i18n)).

- `cities` — `name`, `country`, `flag`, `slug`, `description` (jsonb), `image_url`, `lat`/`lng`, `whatsapp_url`, `active`, `priority`. El grupo de la ciudad es solo **WhatsApp** (no hay campo de Telegram).
- `partners` — `city_id`, `name`, `category` (texto libre: ver `CATEGORY_META` / [deuda técnica](#documentos-y-deuda-técnica)), `description` (jsonb), `image_url`, `lat`/`lng`, `active`, `priority`.
- `partner_links` — los enlaces de cada partner: `partner_id`, `type`, `url`, `label` (jsonb), `sort_order`.
- `partner_events` — eventos/noches: `partner_id`, `theme`, `title`/`description`/`price_label` (jsonb), `image_url`, `starts_at` (timestamptz), `ticket_url`, `priority`, `active`. Alimentan "Trending nights" del home (ambas experiencias, no es una página separada).
- `cta_clicks` — `partner_id` (`NOT NULL`, FK a `partners`, `ON DELETE CASCADE`), `link_type`, `domain`, `path`, `created_at`: un registro por clic en una card de partner del home, en las entradas de un evento, en un resultado de búsqueda (partner o evento), en un enlace del Sheet o en "Cómo llegar". `link_type` mezcla dos cosas: el nombre del evento de tracking (`partner_card_click`, `event_ticket_click`, `search_result_click`, `partner_directions_click`) y, en los clics de enlaces del Sheet, el tipo del propio enlace (`WEBSITE`, `TICKETS`…). No tiene CHECK sobre `link_type`. Se alimenta desde `tracking.js`.
- `admins` — solo `user_id` (Supabase Auth). **Esto es lo único que decide quién puede editar datos** — no basta con iniciar sesión, hay que estar en esta tabla. Las políticas lo comprueban con `private.is_admin()`.
- `profiles` — perfil de cada usuario final registrado (no admin): `id` (mismo `uuid` que `auth.users.id`), `first_name` (nullable), `last_name` (nullable), `city_id`, `university`, `interests` (array de texto: subconjunto de `nightlife`/`housing`/`travel`), `membership_tier` (`free`/`verified`/`plus` — pensado para una futura tarjeta Erasmus unificada, no se fija todavía desde ningún formulario, siempre nace en `free`). Se rellena sola vía trigger, nunca por `INSERT` explícito desde el frontend — ver [Autenticación de usuarios](#autenticación-de-usuarios-loginhtml--registrohtml--recuperarhtml).

**Funciones y triggers**: `public.handle_new_user()` (crea la fila de `profiles` al registrarse), `public.touch_updated_at()` (mantiene `updated_at`) y `private.is_admin()` (en el esquema `private`, no en `public`).

**Edge Function** `translate` (`supabase/functions/translate/`): proxy hacia DeepL para el botón de traducir del admin; exige un JWT válido y guarda la clave de DeepL como secret de la función (nunca en el navegador ni en el repo).

### Seguridad (importante)

Como esta web no tiene servidor propio, la única cosa que protege los datos es la configuración de Postgres (Row Level Security / RLS). Políticas reales:

- **Lectura pública** — `cities`: **todas**, activas o no (a propósito: el directorio completo muestra también ciudades sin grupo activo); `partners`: solo `active = true`; `partner_links`: todos; `partner_events`: solo `active = true` **y `starts_at >= now() - 1 día`** — un evento pasado desaparece solo del sitio al día siguiente (relevante al actualizar fechas: si las dejas en el pasado, no se verán aunque estén activos).
- **Escritura** — solo quien pase `private.is_admin()` puede crear, editar o borrar ciudades, partners, links y eventos. `cta_clicks`: cualquiera (anónimo o no) puede **insertar**, solo un admin puede **leer**. `profiles`: cada usuario solo lee y actualiza **su propia** fila; no hay política de `INSERT` (lo hace el trigger).
- La "anon key" que aparece en `.env.local` es pública a propósito (viaja al navegador de cualquier visitante) — nunca hay que poner ahí la "service role key", que sí es secreta.
- Antes de tocar políticas de RLS o la tabla `admins`, revisar bien el cambio: un error aquí puede dejar la web sin protección de escritura.

### Guardarraíl de sanitización (Capa 1 + Capa 2)

RLS protege quién puede escribir, pero no qué forma tiene el dato una vez guardado — para eso hay dos capas independientes, añadidas después de que un dato de Supabase sin sanear llegara al DOM (regresión real, ya pasada tres veces en este proyecto según el propio comentario de cabecera del script de la Capa 2):

- **Capa 1 — SQL** (`supabase/migrations/20260904000000_add_https_url_check_constraints.sql`, corregida en `20260905000000_fix_https_url_check_constraints.sql`): CHECK constraints que exigen que toda URL guardada en Postgres empiece por `https://` (o esté vacía, en las columnas `NOT NULL DEFAULT ''`) — `partner_links.url`, `cities.whatsapp_url`, `cities.image_url`, `partners.image_url`, `partner_events.ticket_url`, `partner_events.image_url`. Bloquea esquemas ejecutables (`javascript:`, `data:`) a nivel de base de datos, pero **no valida que el destino sea legítimo** ni sustituye al escapado del frontend — un `https://sitio-falso.com` pasa esta validación sin problema.
- **Capa 2 — build** (`scripts/check-sanitization.js`, corre en `npm run build` vía `node scripts/check-sanitization.js && vite build` — hace fallar el build si encuentra algo): recorre `src/js/` y `src/react/` buscando asignaciones a `.href`/`.src`/`.style`/`.style.PROP`/`.innerHTML` cuyo valor no pase por `sanitizeUrl()`/`escapeHtml()` (o `I18n.t()`/`I18n.tField()`, que son texto estático de traducción, nunca dato de Supabase). No es un parser AST de verdad ni un escáner XSS genérico — es un tokenizer de brackets/strings hecho a mano con una lista explícita de excepciones (`EXPLICIT_EXCEPTIONS` en el propio script); su trabajo es no perder de nuevo el patrón de bug ya conocido, no demostrar seguridad matemática. Además comprueba, por cada página HTML que cargue `supabaseClient.js`, que también cargue `src/js/utils/sanitize.js` en algún `<script src>` — y que ese `<script>` no aparezca antes que `supabaseClient.js` ni después de ningún script local que ya llame a `sanitizeUrl()`/`escapeHtml()`, para que esas funciones existan cuando se necesiten.
- `src/js/utils/sanitize.js` es la única fuente de verdad de ambas funciones (`window.escapeHtml`, `window.sanitizeUrl` — sin ES Modules, como el resto del proyecto): `escapeHtml()` para interpolar campos de Supabase dentro de `innerHTML`, `sanitizeUrl()` para validar esquema `http`/`https` antes de usar un valor en `href`/`src` (devuelve un fallback si no es válido). Todas las islas de React que interpolan datos de Supabase (`Card.jsx`, `PartnerCategoryList.jsx`, `PartnerDetail.jsx`) las usan igual que el código clásico. Los enlaces **internos** (relativos, como `ciudad.html?ciudad=1`) no los valida `sanitizeUrl()` (solo acepta http(s) absoluto): `Card.jsx` tiene `safeInternalHref()`, que exige que `to` resuelva al mismo origen con http(s) y bloquea `javascript:`/`data:`/otros orígenes.

## Panel de administración (`/admin`)

Herramienta interna (no aparece en la navegación pública) para gestionar los datos sin tocar código. Un único `admin.js` con toasts, modal de confirmación genérico y un resumen (contadores) en el dashboard:

- **Login**: email + contraseña (Supabase Auth). Solo entran quienes ya tienen cuenta creada y están en la tabla `admins`.
- **Ciudades**: crear, editar, activar/desactivar. El slug se genera automático a partir del nombre.
- **Partners**: crear, editar, activar/desactivar, gestionar sus links (web, entradas, etc.). Desactivar un partner **con algún evento futuro activo** pide confirmación antes (`partnerHasFutureActiveEvent`).
- **Eventos** (`partner_events`): crear, editar, activar/desactivar y **borrar de verdad** (con modal propio). La fecha/hora se edita en hora local del navegador y se guarda como ISO; `priority > 0` destaca la card en el home; la URL de entradas e imagen deben ser `https://` (validado en el formulario y, además, por los CHECK de la base de datos).
- **Traducción con DeepL**: los campos multiidioma (descripción del partner; título, descripción y precio del evento; etiqueta de los links) tienen un botón que rellena la versión EN llamando a la Edge Function `translate`.
- **Extractor de coordenadas**: pegando una URL de Google Maps se rellenan solos los campos de latitud/longitud (`extractCoordsFromGoogleMapsUrl` en `admin.js`).
- **Reporte de clics**: un resumen de `cta_clicks` de los últimos 30 días, agrupados por partner, con el total y su **desglose por tipo** (enlaces, cómo llegar, cards del home, entradas, búsqueda — todo `link_type` que no sea uno de los cuatro nombres de evento cuenta como "enlaces").

Todo el HTML/JS que pinta listas dinámicas en el panel escapa los textos que vienen de la base de datos (función `escapeHtml`, movida de `admin.js` a `src/js/utils/sanitize.js` — ver [Guardarraíl de sanitización](#guardarraíl-de-sanitización-capa-1--capa-2) más abajo) para evitar que un dato mal formado rompa la página o inyecte código.

## Autenticación de usuarios (`login.html` / `registro.html` / `recuperar.html`)

Cuentas de usuario final (no admins) vía Supabase Auth, email + contraseña. Estas dos páginas no aparecían documentadas hasta ahora — ya estaban mergeadas en `main` (rama `feature/auth-profiles`, commit `01f1005`) antes de escribir esta sección.

- **`login.html`** (`src/js/login.js`): email + contraseña. Llama a `signIn()` (`authService.js`); si falla, pinta el error de Supabase tal cual (`error.message`) sobre `#login-form-error`. Si tiene éxito, redirige siempre al literal `'index.html'` — nunca a un `?redirect=` de la URL, a propósito (evitar un open redirect).
- **`registro.html`** (`src/js/registro.js`): email + contraseña (mínimo 6 caracteres, con confirmación — `reg-confirm-password`, valida coincidencia con `reg-password`) + nombre (obligatorio) + ciudad (obligatoria) + apellidos/universidad de origen/intereses (opcionales — apellidos mismo patrón que universidad, un `<input>` de texto sin validación). El formulario está repartido en 3 pasos dentro de la misma tarjeta — ver [Diseño "Living Card"](#diseño-living-card--selector-y-registro-en-pasos) más abajo — pero los campos y su orden lógico no cambiaron: identidad (nombre/apellidos) antes que ciudad. La ciudad **no es un `<select>`** — es un autocompletado de texto libre (mismo patrón que `#citySearch` en `index.js`, reutilizando `.search-dropdown`/`.sdi-*` de `components.css`): el texto visible (`#reg-city`) es solo lo que teclea el usuario, el id real que se envía vive en un input oculto (`#reg-city-id`). Llama a `signUp({ email, password, cityId, university, interests, firstName, lastName })`.
- **`src/js/services/authService.js`**: capa fina sobre `supabase.auth` — `signUp()`, `signIn()`, `signOut()`, `getSession()`. `signUp()` manda `city_id`/`university`/`interests`/`first_name`/`last_name` dentro de `options.data` (metadata del usuario de Auth) — **no** hace ningún `INSERT` a `public.profiles` directamente: eso lo hace un trigger de Postgres (`handle_new_user`, ya aplicado en Supabase) que lee `raw_user_meta_data` al crearse la fila en `auth.users` y crea la fila correspondiente en `profiles` con esas mismas claves (usando `nullif(valor, '')` para que un string vacío llegue como `NULL`, no como `''`). Si se añade un campo nuevo al formulario de registro que deba llegar a `profiles`, tiene que viajar dentro de `options.data` con el nombre de clave exacto que espera el trigger — cualquier otro nombre se pierde en silencio, sin dar error.
- **Confirmación de email obligatoria**: `signUp()` no deja ninguna sesión activa hasta que el usuario confirma su email (Supabase Auth con "Confirm email" activado) — por eso `registro.js` no redirige a ningún sitio tras un alta correcta, sustituye el formulario por un aviso de "revisa tu correo" (`#register-success`). Intentar `signIn()` antes de confirmar devuelve el error `"Email not confirmed"` de Supabase, no un fallo de la app.
- **Icono de cuenta en el nav** (`AuthButton()`, `src/react/navShared.jsx`, usado por `Nav.jsx`/`TopbarNav.jsx`): resuelve `window.getSession()` al montar. Sin sesión (o mientras se resuelve) es un simple `<a href="login.html">`; con sesión, el mismo icono es un avatar con la inicial en mayúscula del **email** (`initialFromSession()` — v1 intencional: no usa `profiles.first_name`, que ya existe, ni una foto de perfil; ver nota de "Personalización con sesión iniciada" más abajo) + un punto verde en la esquina (`--color-success`, primer consumidor real de ese token) que solo se pinta con sesión activa, y abre un desplegable (`.auth-dropdown`) con "Cerrar sesión" (`window.signOut()` + recarga a `index.html` — no se propaga el cambio de sesión entre islas de React, se recarga sin más, mismo criterio que `LangSwitcherButton`). Esto sustituye la nota antigua de este documento sobre `#authBtn` como "placeholder sin login todavía" — ya es funcional de verdad.
- **Validación en cliente y estado de carga** (`login.js`/`registro.js`, ambos con `novalidate` en el `<form>`): email validado en formato (no solo "no vacío") con revalidación en vivo en blur/input; `fieldChecks` es la única fuente de verdad de qué campo falla y por qué, recorrida en el mismo orden que el HTML para mover el foco al primer campo inválido. En `registro.html`, cada paso valida solo sus propios campos antes de dejar avanzar (mismo patrón `fieldChecks`, ver [Diseño "Living Card"](#diseño-living-card--selector-y-registro-en-pasos)); `reg-confirm-password` coincidiendo con `reg-password` (`auth.error_password_mismatch`) se valida en el paso 1, junto con el resto de campos de ese paso — todos solo al avanzar/enviar, mismo criterio que el resto salvo el email, que es el único con validación en vivo. `setFieldError()`/`renderAuthError()` crean el `<p class="auth-error" role="alert">` de un campo solo mientras hay error (nunca reservan hueco vacío) y fijan `aria-invalid` en el input; el error genérico de Supabase (`error.message`) se pinta igual sobre `#login-form-error`/`#register-form-error`. Mientras se espera la respuesta de Supabase, el botón de submit se deshabilita y cambia su texto a `auth.login_submitting`/`auth.register_submitting` — vía `I18n.t()`, nunca capturando el texto original en `DOMContentLoaded` (ese script corre ANTES que `applyTranslations()`, así que capturarlo de la propia página guardaría siempre el español de partida).
- **Mostrar/ocultar contraseña**: un botón (`.auth-password-toggle`, icono Material Symbols `visibility`/`visibility_off`) dentro de `.auth-input-group--password`, junto a `login-password`/`reg-password`/`recover-new-password`. `initPasswordToggle()` (duplicada en `login.js`/`registro.js`/`recuperar.js`, mismo criterio que `normalize()`) alterna `type="password"`/`"text"` del input, el icono y el `aria-label` del botón — este último vía `I18n.t()` en JS, no `data-i18n` (el botón solo tiene un icono como hijo; escribirle `textContent` lo borraría). Mismo patrón visual que el admin (`#toggle-password` en `admin.js`), pero con Material Symbols en vez de los dos SVG propios del admin.
- **Recuperar contraseña** (`recuperar.html` / `src/js/recuperar.js`): un enlace "¿Olvidaste tu contraseña?" bajo el campo de contraseña de `login.html` lleva a `recuperar.html`, que tiene dos estados en la misma página según el hash de la URL:
    1. `#recover-request` (por defecto): pide el email y llama a `resetPasswordForEmail()` (`authService.js`) — Supabase manda un email con un enlace de vuelta a esta misma página. Por seguridad (no filtrar qué emails tienen cuenta), se muestra el mismo aviso de "revisa tu correo" haya o no cuenta con ese email.
    2. `#recover-reset`: se activa cuando la URL trae `type=recovery` en el hash (`#access_token=...&type=recovery`, el formato del enlace del email) — el SDK de Supabase lo detecta solo al cargar (`detectSessionInUrl`, activado por defecto) y deja una sesión temporal activa; `recuperar.js` solo decide qué mitad de la página mostrar mirando ese hash. Pide la contraseña nueva + su confirmación y llama a `updatePassword()` (`authService.js`, `supabase.auth.updateUser({ password })`), que reutiliza esa sesión temporal. Sin sesión válida (enlace caducado o ya usado), Supabase devuelve un error que se muestra igual que el resto de errores de esta familia de formularios. Tras guardar con éxito, redirige a `'index.html'` (no a `login.html`): la sesión de recuperación ya es una sesión real, pedir credenciales otra vez sería redundante.

### Diseño "Living Card" — selector y registro en pasos

Rediseño explícito, no continuista a propósito: antes `login.html`/`registro.html` eran dos páginas visualmente independientes, cada una con su propio eyebrow/h1/p encima de una tarjeta sólida, unidas solo por un enlace de texto ("¿No tienes cuenta? Regístrate" / "¿Ya tienes cuenta? Inicia sesión"). Investigado con la skill `ui-ux-pro-max` (búsquedas `--design-system`/`--domain style`/`--domain product` sobre "social/travel/community app joven") y validado antes con mockups interactivos (Design Component canvas) por las tres direcciones consideradas — se descartaron "Split Identity" (panel editorial fijo) y "Conversacional" (una pregunta a la vez, más fricción para un login rápido) — se implementó **Living Card**: Aurora UI + Glassmorphism, coherente con los tokens de marca ya existentes (índigo/magenta) en vez de una identidad nueva.

- **Selector "Iniciar sesión" / "Crear cuenta"** (`.auth-switcher`, dentro de `.auth-card--glass`, primer hijo): sustituye al enlace de texto — dos `<a>` reales (`login.html`/`registro.html`, navegación normal, `aria-current="page"` en el activo) con una píldora (`.auth-switcher__pill`) que se desliza detrás según `.auth-switcher--register` (modificador puesto en el HTML de registro.html, no vía JS: cada página ya "sabe" cuál es su lado). El color de texto de cada `<a>` es lo único que cambia con el estado, nunca su propio fondo — la píldora es el único elemento que se mueve.
- **Navegación real entre `login.html`/`registro.html`, sin transición propia — dos intentos, los dos descartados a petición**: (1) `.auth-card`, `.auth-switcher`, `.auth-switcher__pill` y los dos blobs del fondo compartiendo `view-transition-name` entre las dos páginas (`transitions.css`), para que el navegador animara la píldora deslizándose y la tarjeta cambiando de alto al navegar — quitado por "no me convence" (`transitions.css` deja un comentario en el sitio exacto donde vivía ese bloque). (2) Después, un fade de salida propio en JS (`initAuthSwitcherFade()`: clic → `preventDefault()` → clase `.auth-card--leaving` con `opacity` → navega en `animationend`) — quitado también, esta vez por "no me gusta", sin más explicación. **No lo vuelvas a intentar sin que se pida explícitamente**: esa navegación se comporta ahora igual que cualquier otra del sitio, con el cross-fade por defecto de `@view-transition { navigation: auto }` (`transitions.css`) que ya existía para todo el sitio, sin nada añadido a propósito para este selector.
- **Aurora que reacciona** (`auth.css`): `body` lleva `auth-page--login` o `auth-page--register` (login.html/registro.html; `recuperar.html` no lleva ninguno, blobs a su opacidad neutra de base) — el blob del color correspondiente gana opacidad. Sin `view-transition-name` (ver el punto de arriba), el cambio de opacidad ya no se interpola al navegar entre páginas — es instantáneo, como el resto del cross-fade por defecto. Deriva lenta continua (`@keyframes auth-glow-drift`, 12s, Aurora UI) en los dos blobs en todo momento, desactivada bajo `prefers-reduced-motion`.
- **Tarjeta de cristal** (`.auth-card--glass`, antes `.auth-card--login`, ahora también en `registro.html` y `recuperar.html` — de ahí el nombre neutro; `.auth-section--login` se renombró igual a `.auth-section--card`): `backdrop-filter: blur(24px) saturate(160%)` sobre `background: color-mix(in srgb, var(--color-surface) 55%, transparent)`, con un `background`/`border` sólido ANTES como fallback de navegadores sin `color-mix()` (dos declaraciones de la misma propiedad; la segunda gana donde hay soporte, la primera es lo único que se aplica donde no lo hay). Ancho fijo deliberado — `max-width: 500px` en las tres páginas (`.auth-section--card`, el wrapper, va a 540px = 500 + los 20px de padding lateral de `.section` a cada lado, para que la tarjeta llegue a tocar su propio máximo de verdad): ni el ancho "de modal" habitual (~400px, se nota estrecho con inputs de una sola columna) ni tan ancho que un email corto quede flotando en un input larguísimo.
- **Registro en 3 pasos** (`registro.js`): paso 1 email+contraseña+confirmación, paso 2 nombre+apellidos, paso 3 ciudad+universidad+intereses+submit real — pensado explícitamente para que quepan más campos en el futuro sin alargar más un único formulario. `goToStep(n, direction)` oculta/muestra el `.auth-step` correspondiente, mueve el foco al primer campo del paso nuevo, actualiza los puntos de progreso (`.auth-step-dot`/`.auth-step-line`) y anuncia el cambio a lectores de pantalla (`#register-step-announcer`, `aria-live="polite"`, reutilizando el propio `textContent` del `<h1>` del paso — nunca un texto duplicado en JS). `direction` ('fwd'/'back') decide si el paso entra deslizándose desde la derecha o la izquierda (`.auth-step--enter-fwd`/`--enter-back`, mismo criterio de continuidad espacial que la píldora del selector) y de qué lado sale. `animateStepHeight()` mide la altura real del `<form>` antes/después de la mutación del DOM y anima entre ambas (mismo patrón FLIP de siempre) en vez de un salto brusco o una altura aproximada fija — desactivado bajo `prefers-reduced-motion`. Cada "Continuar" valida solo los campos de su propio paso antes de dejar avanzar (mismos `fieldChecks`/`setFieldError()` que el resto del formulario); el submit final del paso 3 re-valida TODOS los campos por defensa en profundidad — si encontrara inválido uno de un paso anterior (no debería pasar nunca en el flujo normal, cada "Continuar" ya lo garantiza), `FIELD_STEP` decide a qué paso volver antes de enfocar, para no dejar un error puesto en un campo que no se ve.
- **Traducciones retiradas** (`translations.js`, ya sin ningún consumidor): `auth.register_eyebrow`, `auth.register_subtitle` (el título/subtítulo de registro vivían fuera de la tarjeta; ahora `auth.register_title` es el título del paso 1, dentro de ella) y `auth.already_have_account`/`auth.no_account_yet`/`auth.go_to_login`/`auth.go_to_register` (el `.auth-switch` de texto que el selector sustituye — la clase CSS `.auth-switch` se mantiene, la sigue usando `recuperar.html` para "Volver a iniciar sesión"). `auth.login_title` cambió de copy ("Inicia sesión" → "Bienvenido de nuevo" / "Log in" → "Welcome back"): ya no hace falta que combine con un botón de submit con el mismo texto en la misma pantalla, ambos títulos conviven ahora.

- **Aire simétrico arriba/abajo** (`.auth-page main`): `display:flex; flex-direction:column; justify-content:center` centra la tarjeta en el hueco real entre header y footer (`main` ya es `flex:1` en el `<body>` flex-column de `base.css`) — sin esto, la tarjeta quedaba pegada arriba (solo el padding-block de `.section`) con todo el sobrante como aire suelto debajo, creciendo con la altura de pantalla. Gotcha real ya resuelto: convertir `main` en flex contenedor hace que `.section` (con su `margin: 0 auto` de toda la vida) deje de estirarse al ancho de `main` y se encoja a su contenido — un `margin` automático en el eje transversal de un flex item desactiva el `stretch` por defecto. `.auth-page main > .section { width: 100% }` lo corrige explícitamente; sin esa regla, `max-width` en `.auth-section--card`/`.auth-card--glass` nunca llegaba a alcanzarse de verdad (la tarjeta se veía más estrecha que su propio límite declarado).

### Personalización con sesión iniciada

Dos retoques de UI ligados a `getSession()`, en páginas y componentes ya existentes (no son una isla de React nueva ni una página nueva):

- **Avatar + punto verde** — ver el bullet de `AuthButton()` justo arriba. CSS en `src/css/components.css`, bloque `.icon-btn`/`.auth-avatar`/`.auth-status-dot` (`.icon-btn` necesitó `position: relative` nuevo, antes solo lo tenía `.auth-menu`, para anclar el punto en su esquina).
- **Saludo personalizado en el H1 del home** (`index.html`, `initHeroTitleAnim(session)` en `src/js/index.js`): con sesión, sustituye el titular genérico por `"(parte del email antes de la @, con mayúscula inicial), (auth.hero_greeting_suffix traducido)"` — `displayNameFromEmail()`, literal, nunca traducido, a diferencia del sufijo. La sustitución vive **dentro** de `initHeroTitleAnim()`, en el mismo `forEach` que trocea el texto en palabras para la animación — cualquier cambio de texto fuera de ese punto exacto se pierde o rompe la animación, porque la función quita `data-i18n` de cada `<span>` al terminar. `DOMContentLoaded` ahora hace `const session = await getSession(); initHeroTitleAnim(session);` antes de llamarla.
- Ambos usan la inicial/parte local del **email**, no `profiles.first_name`/`last_name` (que ya existen desde el registro con nombre/apellidos, ver más abajo) — mismo motivo v1 en los dos sitios: intencional, no una implementación a medias, con margen para pasar a usar el nombre real más adelante si se decide.

### Requisito de configuración en Supabase — no es un bug de código

El proyecto de Supabase (`puivkbjgbfnlpepyednt`) necesita **"Allow new users to sign up"** activado en el dashboard (**Authentication → Sign In / Providers**, o **Authentication → Settings** según la versión) para que `registro.html` funcione. Con ese ajuste desactivado, `signUp()` devuelve `HTTP 422 — "Signups not allowed for this instance"` para cualquier alta, sin llegar a tocar `auth.users` ni `profiles` — el frontend/JS no tiene ningún fallo en ese caso, es puramente configuración del proyecto de Auth. Ya se encontró apagado una vez (por defecto, o desactivado en algún momento) y hubo que activarlo a mano desde el dashboard — si el registro público vuelve a fallar con ese mismo mensaje, revisar aquí antes que el código.

**Verificado end-to-end** (18 sept. 2026, con "Allow new users to sign up" ya activado): registro real por el formulario → fila creada en `auth.users` → trigger `handle_new_user` crea la fila en `profiles` con `city_id`/`university`/`interests` correctos → login antes de confirmar el email falla con `"Email not confirmed"` (esperado) → tras confirmar el email, el login funciona y crea una sesión real (`getSession()` la devuelve).

**Otro límite de Supabase con el que tropezar, mismo espíritu que el anterior**: el envío del email de confirmación tiene un rate limit propio de la cuenta (`HTTP 429 — "email rate limit exceeded"`) — salta si se hacen varios registros de prueba seguidos en poco tiempo, antes de que `auth.users` llegue a crear nada. Tampoco es un bug de `signUp()`/`registro.js`; si aparece este mensaje probando en local, esperar o espaciar los registros de prueba.

**Tercer requisito, mismo espíritu, para `recuperar.html`**: `resetPasswordForEmail()` manda `redirectTo: window.location.origin + '/recuperar.html'` — esa URL (por dominio: local, Vercel Preview y producción son orígenes distintos) tiene que estar en la lista de **Redirect URLs** del proyecto (**Authentication → URL Configuration**). Si no lo está, Supabase igualmente manda el email (sin error visible en `resetPasswordForEmail()`) pero el enlace redirige a la Site URL por defecto en vez de a `recuperar.html?...#type=recovery`, y `recuperar.js` nunca detecta el estado de "fijar nueva contraseña". Sin verificar todavía end-to-end (a diferencia del registro) — si el flujo de recuperación falla en producción sin ningún error de código, revisar aquí antes que `recuperar.js`/`authService.js`.

## Internacionalización (i18n)

Dos idiomas, **español (`es`) e inglés (`en`)**. No hay librería (i18next, etc.): un helper mínimo, `window.I18n` (`src/utils/i18n.js`).

- **Idioma activo** — `localStorage['lang']` manda; si no hay nada guardado, se usa el idioma del navegador (`navigator.language`), y **cualquier idioma que no sea `es` cae a `en`**. Cambiar de idioma **recarga la página completa** (`langSwitcher.js` / `LangSwitcherButton` en el nav guardan el idioma y hacen `location.reload()`): nada se reescribe en caliente ni se observa el DOM.
- **Dos tipos de texto, dos mecanismos distintos**:
    1. **Texto de interfaz** (estático, vive en el repo): `src/utils/translations.js` (`window.I18n.translations = { es: {…}, en: {…} }`, claves anidadas `sección.clave`, p. ej. `nav.services`). En HTML se marca con `data-i18n="nav.services"` y `I18n.applyTranslations()` lo resuelve una vez tras `DOMContentLoaded` (en `INPUT`/`TEXTAREA` escribe `value`, o `placeholder` si lo tiene). En JS se usa `I18n.t('clave')`. Si una clave falta en el idioma activo cae al español, y si tampoco existe, **devuelve la propia clave** (un `data-i18n` mal escrito se nota a simple vista en vez de fallar en silencio). **Al añadir un texto nuevo, añádelo a `es` y a `en`.**
    2. **Texto de datos** (viene de Supabase): campos **JSONB `{ "es": …, "en": … }`** (`partners.description`, `cities.description`, `partner_links.label`, `partner_events.title`/`description`/`price_label`). Se leen con `I18n.tField(valor)` (idioma activo → `es` → `en` → `''`; tolera un string plano legacy). `fetchPartnersByCity()` (`partnersService.js`) ya devuelve `description` y `label` de los links traducidos, así que los componentes React los reciben listos; los eventos (`title`, `price_label`…) los traduce `nightsSection.js` al construir cada card.
- **En las islas de React**, los textos se resuelven **al renderizar** con `t(clave, fallback)` de `navShared.jsx` (que llama a `window.I18n.t`), **no** con `data-i18n` + `applyTranslations()`: un script clásico enganchado a `DOMContentLoaded` puede correr antes de que React monte (ver la regla de arquitectura en [Navegación](#navegación)). Los datos multiidioma que pinta `servicesData.js` llevan una clave de i18n y un `fallback` en español por si `I18n` no estuviera cargado.
- **Carga** — `i18n.js` y `translations.js` son scripts clásicos; se copian tal cual al build con `vite-plugin-static-copy` (`src/utils/**/*`), porque no pasan por Vite (sin ese target el build de producción los omite y dan 404, aunque `npm run dev` no lo detecta).
- **Admin** — los campos JSONB se editan con dos cajas (ES/EN) y un botón «traducir» que rellena la EN vía la Edge Function `translate` (DeepL) — ver [Panel de administración](#panel-de-administración-admin).

## Experiencia dual (Verified / Parties)

`src/js/experience.js` es el primer script que carga cada página. Decide qué "experiencia" mostrar:

- Mirando el dominio (`erasmusparties.org` → experiencia "Parties"; cualquier otro dominio → "Verified", que es la experiencia por defecto).
- Se puede forzar en local añadiendo `?exp=parties` o `?exp=verified` a la URL, sin tener que cambiar de dominio.

El resultado se guarda en `window.ERASMUS_EXPERIENCE` para que el resto de scripts lo puedan leer, y se añade una clase (`theme-verified` o `theme-parties`) al `<html>` para pintar los colores correctos.

En la experiencia "Parties" además se ocultan los enlaces a Servicios/Alojamiento/Viajes y el logo cambia a "Erasmus Parties"; se añade un enlace "Verified ↗" para volver a la web completa. Esto lo resuelven directamente los propios componentes React leyendo `window.ERASMUS_EXPERIENCE` al renderizar — no solo el nav (`Nav.jsx`/`TopbarNav.jsx`, ver [Navegación](#navegación)), también el footer (`Footer.jsx`, ver [Footer](#footer-footerjsx)) y el bottom-nav móvil (`AppShell.jsx`, ver [Bottom-nav](#bottom-nav-móvil-768px)). `experience.js` sigue mutando el DOM tras `DOMContentLoaded` como antes, pero solo le queda margen real sobre HTML suelto que ninguno de esos tres componentes gestiona — por ejemplo el enlace a `alojamientos.html` de la sección de servicios del home (`index.html`, fuera de nav/footer/bottom-nav) — porque cada uno de los tres se marca con `data-react-nav`/`data-react-footer` en su raíz y `experience.js` respeta ese guard para no tocar dos veces lo que React ya resolvió (ver la regla de arquitectura en [Navegación](#navegación)).

En la experiencia Parties, `alojamientos.html` **redirige a `index.html`** (su `<script>` inline lo hace antes de pintar): esa sección no existe en esa marca, igual que se ocultan por CSS `.alojamiento-section`, `.services-section` y `.cities-section` del home.

## Cómo añadir datos

Nueva ciudad, partner o evento: usar el panel de administración en `/admin` (ver sección de arriba) — es la única forma, no hay ninguna página que siga leyendo de un archivo estático. Los datos quedan en Supabase y aparecen automáticamente en home, ciudad, ciudades del país, listado completo y mapa. Ver los skills `/add-city` y `/add-partner` si quieres que Claude te guíe paso a paso con los campos exactos del formulario.

## Mapa interactivo (`mapa.html`)

El mapa Leaflet vive **solo en `mapa.html`** (a pantalla completa, con la lista de partners al lado). `ciudad.html` **ya no tiene mapa**: sustituyó el mapa embebido + panel arrastrable (CitySheet) por una lista de partners con tarjetas y un enlace a `mapa.html` — ver [Ciudad sin mapa](#ciudad-sin-mapa-ciudadhtml). Por eso `mapPartners.js` y `cityMap.js` son ahora de `mapa.html`, y la lista de `ciudad.html` la gestiona `cityPartners.js`.

### `mountCityMap(containerId, { pais, ciudad, lat, lng, interactive })`

- `mapa.js` la llama con `interactive` por defecto (`true`); la altura del mapa la fija JS (`window.innerHeight - altura del header`) y se resincroniza en `resize`.
- `interactive: false` (mapa bloqueado con overlay `.city-map-activate` "toca para interactuar") sigue soportado en `cityMap.js`, pero **hoy ninguna página lo usa**: se introdujo para el mapa embebido de `ciudad.html` en móvil, donde Leaflet secuestraba el scroll de página con `preventDefault()` en `touchmove`. Si algún día vuelve a haber un mapa dentro de una página que hace scroll, hay que usarlo — se probó a quitarlo y era un fallo real, confirmado en el navegador.
- En móvil, `mapa.html` usa `.map-with-list` (`mapa.css`): mapa `flex: 1` + lista con scroll propio debajo, a altura de página completa. No comparte el problema de scroll de antes, porque no hay contenido de página más allá del propio mapa.

### Ciudad sin mapa (`ciudad.html`)

`ciudad.js` (orquesta la página) + `cityPartners.js` (la lista) + `CityPartnerList.jsx` (las cards). Se quitó el mapa embebido en el PR #81 y esta sección sustituye a las antiguas "Panel arrastrable (CitySheet)" y "Buscador local"; `citySheet.js` ya no existe.

- **Orden de la página**: `<h1>` con el nombre + buscador local → **`.city-quick-actions`** (grupo de WhatsApp + enlace al mapa, ver más abajo) → consejo → `#city-extra` (enlace a `alojamientos.html` y, si la ciudad tiene partners `travel`, sus "escapadas" como `ServiceCards`; ver [Familia de cards](#familia-de-cards-card)) → lista de partners (`#city-partners`). El botón "Inicio" del header ya no existe.
- **CTA de WhatsApp arriba, no un banner** — a petición explícita de Álvaro: antes vivía dentro de `#actionsWrap` pero CONCATENADO DESPUÉS del bloque de partners en el HTML generado (`buildPartnersBlock(city) + divisor + botón`), así que aunque `#actionsWrap` estuviera arriba en el DOM estático, el botón acababa apareciendo muy abajo en pantalla una vez pintada la lista de partners (más larga aún con "Destacados"/categorías, ver más arriba). Ahora `ciudad.js` construye primero `.city-quick-actions` (el CTA de WhatsApp + `buildMapLink()`, el enlace "ver mapa a pantalla completa" que antes vivía dentro de `buildPartnersBlock()`) y lo antepone al bloque de partners.
- **Familia `.city-action-btn` — "acciones rápidas" de verdad, no dos pills sueltas** — con dos formas según haya hover real o no, ninguna dependiente de la otra:
    - **Con hover real** (`@media (hover: hover) and (pointer: fine)`, el mismo criterio que ya usa `.card-cta--info` en `card.css` para esto mismo): pill completa (icono + `.action-label`, la frase larga) que colapsa en reposo a un círculo de solo icono (`max-width: 44px`, padding recalculado para que icono+padding sumen exactamente 44px) y se expande de vuelta a la pill al pasar el ratón o enfocar (`:hover`/`:focus-visible` — nunca solo `:hover`, para que quien navegue con teclado también pueda "ver" el botón) — "aumentable", a petición explícita.
    - **Sin hover real (mobile-first, por defecto — pedido después, explícitamente: "es importante saber que no hay hover en móvil")**: nunca se le pide a alguien sin ratón que descubra un botón que depende de un hover que no puede disparar, así que en vez de dejar la pill expandida fija (única alternativa "seguro" pero no una propuesta de diseño real), se rediseñó como **tile** — un `.action-icon` en círculo de color (44px) con una **caption corta siempre visible debajo** (`.action-caption`: "WhatsApp"/"Mapa", nunca la frase larga completa — en una columna de ~70-90px de ancho no cabría sin desentonar), dos tiles en fila (`.city-quick-actions` pasa de columna a fila mobile-first, liberando además scroll vertical antes de llegar a "Destacados"). El color en este modo lo lleva el círculo del icono, no todo el `<a>` (que aquí es solo un contenedor transparente en columna) — al contrario que en el modo con hover, donde el color lo lleva la pill entera. Las dos formas comparten color vía custom properties (`--action-color`/`--action-color-hover`, definidas una vez por modificador `--whatsapp`/`--map`) en vez de duplicar la paleta por contexto — la propia idea de "familia" pedida, no dos componentes sueltos.
    - `--whatsapp` (verde `--wa`) y `--map` (indigo `--primary`) son los dos modificadores; `buildMapLink()` dejó de usar `.city-partners-map-link` (borrada, sin otro consumidor) para pasar a esta familia.
    - **Bug real encontrado y corregido en el propio desarrollo**: sin un `.city-action-btn:hover { color: #fff }` explícito (dentro del bloque con-hover, donde el texto SÍ es blanco sobre color), la regla global `a:hover` (`typography.css`, `color: var(--color-primary-hover)`) ganaba por especificidad sobre el `color:#fff` de ese contexto — mismo bug ya conocido y documentado para `.btn-primary-pill:hover` en `components.css`, aquí repetido antes de corregirlo; verificado con Playwright (`getComputedStyle` en pleno `:hover`) que el texto del botón de mapa era del mismo tono indigo que su propio fondo, prácticamente invisible, y que tras el fix pasa a blanco de verdad.
    - `aria-label` en el `<a>` repite siempre la etiqueta completa: ni `.action-caption` (corta, visible en móvil) ni `.action-label` (larga, visible solo al pasar el ratón en escritorio) están visibles todo el tiempo, así que el nombre accesible no puede depender de ninguna de las dos — las dos llevan `aria-hidden="true"`.
    - El botón del estado "sin partners" (`renderNoPartnersState()`, `cityPartners.js`) reutiliza la misma clase `--whatsapp` e `iconWa()` (global de `ciudad.js`, ya cargado en `ciudad.html` para cuando esta función se ejecuta) — mismo comportamiento en los dos sitios de la página, incluida la caption corta.
    - **El equivalente de `mapPartners.js` (mismo texto, mismo `renderNoPartnersState()`, pero en `mapa.html`) sigue en `.btn-primary-pill` puro (indigo, sin el tratamiento de esta familia)** — página distinta, `ciudad.css`/`iconWa()` no están cargados ahí; pendiente si Álvaro pide la misma unificación en `mapa.html`.
    - Sin `whatsapp_url`, `buildGroupPending()` sigue pintando un chip informativo compacto (`.city-group-pending`, sin tocar en este cambio — no es una acción, así que no forma parte de esta familia interactiva).
    - De paso se retiró la rama `platform: 'telegram'`/genérico de `buildGroupBtn()` (ahora `buildWhatsappCta()`, WhatsApp-only): el único llamador real siempre pasaba `'whatsapp'` — `iconTg()`/`iconGeneric()`/`.btn-tg`/`.btn-generic` eran código muerto inalcanzable, y las traducciones `join_groups_divider`/`join_telegram_channel`/`whatsapp_group_label`/`groups_coming_soon_prefix`/`common.check_back_soon` se retiraron con ellos.
    - **Verificación de la versión sin hover, nota de tooling**: la sesión de Playwright/Chromium disponible reporta `(hover: hover)`/`(pointer: fine)` como verdaderos incluso a un viewport de 390px (sin emulación real de dispositivo táctil vía las herramientas expuestas, y `browser_emulate_media`/CDP `Emulation.setEmulatedMedia` con `features: [{name:'pointer'|'hover', ...}]` no lo cambia) — un simple resize no basta para ver la versión táctil en este entorno. Verificado en su lugar forzando `rule.media.mediaText = 'not all'` sobre el propio `CSSMediaRule` de `(hover: hover) and (pointer: fine)` sobre las hojas ya cargadas (vía `document.styleSheets`, recorriendo también `CSSImportRule.styleSheet` porque `styles.css` es un manifiesto de `@import`), para simular de verdad "sin hover" y confirmar el tile por captura de pantalla.
- **Lista** — `mountCityPartners(containerId, city, { autoOpenPartnerId })` pide los partners (`fetchPartnersByCity`), los agrupa por categoría y devuelve `{ listGroups, selectPartner, activateOnlyCategory }` (la misma forma que `mountPartnersList()` de `mapPartners.js`, a propósito). El estado es otro que en el mapa, porque no hay pines que filtrar: `collapsedCategories` (plegar/desplegar una categoría, solo visual) y `focusCategory` ("ver todo de esta categoría sola"). Cada categoría enseña como mucho **6 cards** (`CAP` en `CityPartnerList.jsx`) y un botón "ver todo (+N)" que entra en modo foco, con vuelta atrás; con una sola categoría no hay control de plegar. Se duplicó `mapPartners.js` en vez de convertirlo en un "si hay mapa / si no": cuando dos contextos divergen de verdad, se copia (mismo criterio que `isPartiesExperience()`/`normalize()`).
- **Experiencia Parties**: las categorías que no son `nightlife` arrancan **plegadas**, para no enterrar lo propio de esa marca bajo lo de otras categorías. Un deep link (`?partner=ID`, o el resultado del buscador global de `index.js`) las despliega si hace falta.
- **Destacados + categorías plegadas (`showHighlights`, siempre en Verified)** — mockup acordado con Álvaro para "de un vistazo es confuso, demasiada información de golpe": mostrar TODAS las categorías desplegadas de golpe era justo el problema. `initialCollapsedCategories()` pliega todas las categorías menos la más numerosa (empate: gana la primera en orden original) y `cityPartners.js` calcula `highlights` — el primer partner (`fetchPartnersByCity` ya ordena por `priority` desc) de hasta 4 categorías distintas, para variedad en vez de "los 4 de mayor priority" que podrían salir todos de la misma categoría. `CityPartnerList.jsx` pinta, antes de la lista: una franja "Destacados" (mismas `Card`/`.city-partners-grid` que cualquier categoría — el solape con lo que ya se ve dentro de su categoría al desplegarla es a propósito, mismo patrón que cualquier fila de "destacados" sobre un listado por categorías) y una fila de pastillas de categoría (icono + nombre + contador) que entran en modo foco al pulsarlas (`onEnterFocus`, la misma función del botón "ver todo"). Ambos bloques se ocultan en modo foco. **Se aplica siempre en Verified, a petición explícita — incluso con 1 sola categoría/1 solo partner** (consistencia visual por encima de ahorrarse una franja "Destacados" redundante con un único elemento; primera versión de esto solo se activaba con >3 categorías, se quitó ese umbral tras verlo en real con la única ciudad con datos, Bilbao, 3 categorías). Parties conserva su propio criterio de arranque (solo `nightlife` desplegada) sin tocar.
- **Cards** — cada partner es un `Card layout="compact"` (avatar o monograma con el color de la categoría, categoría, distancia, descripción a 2 líneas) con dos botones: "Cómo llegar" (enlace directo a Google Maps con las coordenadas; **no se pinta sin `lat`/`lng`**) y "Ver más", que abre el mismo Sheet de siempre (`sheet.js` + `PartnerDetail.jsx`). "Cómo llegar" dispara `trackEvent('partner_directions_click')` y los enlaces del Sheet `partner_link_click` — ambos se guardan en `cta_clicks` (ver `tracking.js`); "Ver más" solo abre el Sheet.
- **Distancia** — `getUserLocation()` (`utils/geolocation.js`) se pide sola al cargar la lista, **sin bloquear el primer render**: cuando resuelve, se calcula `distanceMeters` y se repinta. Sin permiso o soporte devuelve `null` y no sale ninguna distancia (nunca un "-- m").
- **Buscador local** (`initCitySearch()` en `ciudad.js`) — mismas clases que el de `index.html` (`.search-bar`/`.search-dropdown`/`.sdi-*`, genéricas en `components.css`) pero busca **tipos de partner y partners de esta ciudad**. Oculto (`hidden`) hasta que `mountCityPartners()` devuelve un handle, es decir, si la ciudad tiene partners. El índice sale de `listGroups` (en memoria, cero red): una entrada por categoría y una por partner, filtradas por substring normalizado (acentos/mayúsculas) y ordenadas por prefijo → categoría antes que partner → alfabético. Elegir una **categoría** llama a `activateOnlyCategory` (entra en modo foco) y elegir un **partner** a `selectPartner(id)` (la misma función del deep link `?partner=ID`: despliega su categoría si hace falta y abre su Sheet). La lista de `mapa.html` conserva el mismo contrato, con `activateOnlyCategory` = "solo estos pines".
- `--topbar-h` sigue inyectándose en `ciudad.js` (altura real de `header.topbar`); hoy solo la lee `layout.css` (`padding-top` del menú móvil `.mobile-nav`).

### Geocodificación

`src/js/geocoder.js` llama a la API pública de Nominatim (OpenStreetMap) solo si una ciudad no tiene ya coordenadas guardadas en Supabase, luego las almacena en localStorage. No hay rate limiting implementado — añadir ciudades una a una si se geocodifican en batch.

### Tiles del mapa

CARTO Light (`light_all`) vía CDN. La atribución a OpenStreetMap + CARTO es **obligatoria por licencia** y ya está incluida en `initMap()`.

**Motivo del error "API KEY REQUIRED"**: hasta finales de agosto de 2026, `basemaps.cartocdn.com` servía las tiles ráster de forma anónima y gratuita, sin necesidad de cuenta ni key (así se montó originalmente el proyecto). Ese mes CARTO cambió su política — sin previo aviso al proyecto — y empezó a exigir una API key para ese mismo endpoint; las peticiones sin key pasaron a servirse con una marca de agua "API KEY REQUIRED" sobre el mapa. No fue un cambio de código ni de tráfico del proyecto, sino una decisión de CARTO (confirmado por reportes idénticos de otros proyectos —Home Assistant, openHAB, Grafana— exactamente esas mismas fechas). CARTO además avisa de que este servicio ráster (PNG) se irá retirando a medio plazo en favor de su nuevo servicio de tiles vectoriales.

**Solución aplicada**: pedir una API key gratuita de CARTO (gratis hasta 5M tiles/mes, sin tarjeta — [carto.com/basemaps/apikey](https://carto.com/basemaps/apikey/)), gestionada con la cuenta **suarez91alvaro@gmail.com**. Se configura como `VITE_CARTO_API_KEY` en `.env.local`; `initMap()` la lee de `window.__CARTO_API_KEY__` (inyectada por `vite.config.js`) y la añade como parámetro `?key=` a la URL de tiles, que también pasó a usar la ruta `rastertiles/light_all/...` (formato nuevo exigido junto con la key, sustituye a la ruta antigua `light_all/...`).

**Alternativas consideradas y descartadas** (por si esta key deja de ser viable en el futuro):

- **Proveedor gratuito sin cuenta** (Esri "World Light Gray Base" u OpenStreetMap estándar): sin key ni registro, pero cambia ligeramente el aspecto visual (gris en vez del blanco actual) y depende de las políticas de uso gratuito de ese proveedor, que también podrían cambiar sin aviso.
- **Migrar a Google Maps JavaScript API**: Google no ofrece tiles sueltas compatibles con Leaflet (no hay URL de tiles oficial tipo `.../{z}/{x}/{y}.png`, solo su propia librería `google.maps.Map`) — usar sus tiles con Leaflet requeriría URLs no oficiales que incumplen sus términos de servicio. La vía legítima implicaría sustituir Leaflet por la Google Maps JS API (cambio de código más grande, aunque contenido en `map-helpers.js` por diseño) y una cuenta de Google Cloud con facturación activada (tarjeta), con ~$200/mes gratis y pago por uso después.

### Por qué el mapa no se migró a React

El plan de migración a islas de React (ver [Familia de cards](#familia-de-cards-card) y [Lista de partners](#lista-de-partners-partnercategorylist) más abajo) incluía en algún momento el bloque del mapa — se revisó y se decidió **no migrarlo**, ni ahora ni como línea de trabajo futura, por varios motivos que juntos hacen que no compense:

- `cityMap.js`, `map-helpers.js` y `geocoder.js` son infraestructura pura (arrancar Leaflet, geocodificar direcciones, cachear coordenadas) — no pintan contenido que crezca o cambie con los datos (texto, fotos, listas), que es justo el patrón común a las cuatro islas ya migradas. Sin ese patrón, no hay nada que React resuelva mejor que el DOM imperativo que ya hay.
- Los pines de partner (`createPartnerMarker`/`setMarkerExpanded` en `map-helpers.js`) son un `divIcon` de Leaflet construido con una sola línea de HTML (icono + color) — Leaflet exige ese HTML como texto plano, así que montar un componente React ahí no simplificaría nada: añadiría un paso de renderizado extra para acabar produciendo exactamente el mismo string.
- El mapa en sí está estable — sin cambios de diseño pendientes ni problemas reales reportados (confirmado explícitamente por Álvaro antes de tomar esta decisión, no una suposición).
- Reescribirlo de todas formas sería tocar código que ya funciona bien sin ningún beneficio para quien usa la web — el mismo criterio de no añadir complejidad que no resuelve un problema real que ya se ha aplicado en el resto del proyecto.

## Familia de cards (Card)

Todas las cards de contenido de la web (partners y eventos del home, partners de ciudad, alojamientos y viajes, servicios, ciudades) son miembros de **una sola familia**: el componente React `Card.jsx`, con slots opcionales, cinco `layout` y un catálogo de tipos de CTA. Ninguna se construye ya a mano con `innerHTML` ni se copia como HTML estático. El grid de partners/eventos del home fue la primera (la **segunda isla de React** del proyecto, tras el menú, y la primera que pintó contenido real de datos); el resto se fue migrando por fases (ver abajo). Único caso fuera de la familia, a propósito: el acordeón de ciudades del home (`accordion-card`).

### Fases de la familia de cards

La idea es que TODAS las cards de la web sean miembros de la misma familia (`Card.jsx`), distinguidas por qué slots, qué `layout` y qué `cta.kind` llevan. **Fase 1 (hecha)**: partner-card + event-card del home. **Fase 2 (hecha)**: `city-partners-card` de `ciudad.html` (`CityPartnerList.jsx` ahora solo traduce partner → props de `Card` con `layout="compact"`). **Fase 3a (hecha, luego retirada)**: `collab-card` de alojamiento, viajes y la sección de alojamiento del home (12 `<a>` estáticos → `CollabGrid.jsx` + `collabData.js`, `Card` con `layout="tile"`; en una revisión posterior pasó al layout por defecto (`stacked`) + CTA `offer`). Esta fase completa se **retiró del proyecto** más adelante: se dejó de distinguir alojamiento/viajes de terceros de los gestionados por Erasmus Verified (decisión explícita de Álvaro — ver [Sin distinción de terceros](#sin-distinción-de-terceros--todo-gestionado-por-erasmus-verified)), así que `CollabGrid.jsx`/`collabData.js`/`mount-collab-grid.jsx` ya no existen. **Fase 3b (hecha)**: `service-card` de `servicios.html` (6 estáticas) y de `ciudad.js` (escapadas de la ciudad, dinámicas) → `ServiceCards.jsx` + `servicesData.js`, `Card` con `layout="service"` y CTA `offer`. **Fase 3c (hecha)**: las 3 cards de servicios del home (`index.html`, `data-services-grid="home" data-no-animate`) pasan al mismo `ServiceCards`; se eliminó el CSS legacy `.service-card`/`.service-icon`/`.service-name`/`.service-desc`/`.service-link` de `components.css`. Sus enlaces eran `href="#"` muertos: ahora «Explorar anuncios» → `alojamientos.html` y «Pedir ahora» → `servicios.html` (CTA `link`, misma pestaña), y la card de **Seguro de intercambio no lleva botón** hasta que haya página o proveedor (añadir `to` + `ctaKey`/`ctaFallback` en el set `home` de `servicesData.js`). **Fase 4 (hecha)**: `city-card` de `ciudades.html` y `ciudades-todas.html` → `CityCards.jsx` + `Card` con `layout="photo"`. Las cards del acordeón del home (`accordion-card`, `index.js`) **quedan fuera a propósito**: no son cards estáticas sino un patrón de interacción propio (ensanchamiento + pin & scrub al hacer scroll) con su CSS específico. Con esto, la familia cubre todas las cards de contenido de la web.

### Qué hace cada archivo

| Archivo                                                                    | Qué hace                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/react/Card.jsx`                                                       | La card única de la web. Slots **opcionales**: `imageUrl`/`imageAlt`/`monogram`/`badge`, `title`/`meta` (+`metaIcon`)/`date`/`price`/`description` y `ctas: [{ kind, label, href? \| to?, onClick? }]` (o `cta` suelto; `href` = externo http(s) en pestaña nueva, `to` = interno de mismo origen, validado por `safeInternalHref()`). Un slot sin valor no se renderiza — cada tipo de card es una combinación de slots, no un componente aparte. Prop `layout`: `'stacked'` (defecto, imagen 4:3 arriba — home), `'compact'` (avatar 44px junto al título, con borde — partners de ciudad; `accent` = color CSS que tiñe badge, monograma y hover) `'tile'` (nombre + descripción centrados, toda la card es un `<a>` con `href`/`rel`, sin CTA; sin `href` válido no se renderiza — sin consumidor activo hoy, ver [Reglas al tocar o ampliar la familia](#reglas-al-tocar-o-ampliar-la-familia-salidas-de-la-revisión-final)) `'service'` (icono en cuadro + título + `highlight` en negrita + descripción + CTA — servicios) o `'photo'` (foto a sangre 3:4 con degradado, título + `badge` verde o `meta` de texto abajo; toda la card es un `<a>` **interno** con `to` — validado como mismo origen por `safeInternalHref()`, no por `sanitizeUrl()`, que solo acepta http(s) absoluto —; `arrow` añade la flecha de esquina y `hover` es `'zoom'` (defecto) o `'reveal'`, foto en gris que pasa a color — ciudades). `CTA_KINDS` es el único sitio donde se define cada tipo de botón: `info` (enlace de texto, se revela en hover con puntero fino), `tickets` (botón relleno siempre visible), `directions` (secundario, enlace a Google Maps) `details` (primario; sin `href` pero con `onClick` se renderiza como `<button>`) `offer` (reutiliza el botón global `.btn-primary-pill`) y `link` (texto + chevron a una página **interna**, `to`, misma pestaña); un tipo nuevo = una entrada ahí + su modificador `.card-cta--<kind>` en `card.css`. Sanea `imageUrl`/`cta.href` con `sanitizeUrl()`. Estilos en `src/css/card.css`; lo que depende del contenedor (carrusel/grid de eventos, `.card--featured`) sigue en `home.css` bajo `.events-scroll .card`. |
| `src/react/SummaryCardGrid.jsx`                                            | Itera una lista de `items` y monta un `<Card>` por cada uno. No sabe qué es un partner ni un evento — esa traducción la hace `getCardProps(item, index)`, una función que le pasa el padre (`getPartnerCardProps`/`getEventCardProps`, ver abajo).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/react/ServiceCards.jsx`, `servicesData.js`, `mount-service-cards.jsx` | Cards de servicios (`Card layout="service"`). `servicesData.js` es el único sitio con los 6 servicios de `servicios.html` (sets `sim`/`bank`/`transport` (servicios.html) y `home` (index.html, con `nameKey` traducido y `to` interno); claves i18n + fallbacks; `name` literal en los de servicios.html). `mount-service-cards.jsx` tiene **dos vías**: auto-monta sobre cada `<div data-services-grid="sim\|bank\|transport\|home" [data-no-animate]>` (resolviendo textos con `t()`), y expone `window.mountServiceCards(containerEl, items)` para `ciudad.js`, que pinta el grid con `innerHTML` y monta las cards DESPUÉS sobre `#city-travel-grid` (root efímero: devuelve `{ unmount() }`). `ServiceCards` recibe los textos ya resueltos y dispara `initScrollReveal()` en un `useEffect`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/react/CityCards.jsx`, `mount-city-cards.jsx`                          | Cards de ciudad (`Card layout="photo"`). `CityCards` recibe los textos ya resueltos (`{ name, to, imageUrl?, tag?, meta?, delay? }`) y dispara `initScrollReveal()` en un `useEffect`. `mount-city-cards.jsx` expone `window.mountCityCards(containerEl, items, { arrow, hover })` → `{ unmount() }`. **Root efímero**: `ciudades-todas.html` regenera los grupos de letra por completo en cada tecleo del filtro, así que `renderGroups()` hace `unmount()` de los roots anteriores ANTES de quitar sus contenedores (verificado: mounts/unmounts casan, sin fugas); `ciudades.js` monta una sola vez sobre `#citiesGrid`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/react/mount-summary-cards.jsx`                                        | Expone `window.mountSummaryCards(containerEl)` — el puente para que `index.js`/`nightsSection.js` (scripts clásicos, sin `import`) creen un root de React sobre `#partnerGrid`/`.events-scroll` **una única vez** y vuelvan a pintar sobre ESE MISMO root en cada cambio de filtro, sin destruirlo y recrearlo. Es el primer puente module→script-clásico del proyecto: hizo falta porque, a diferencia del nav (que se monta una vez y no vuelve a renderizarse), estas tarjetas sí necesitan repintarse repetidamente desde código clásico.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

`renderPartnersSection()` (`index.js`) y `renderEventCards()` (`nightsSection.js`) arman las props de cada card con `getPartnerCardProps()`/`getEventCardProps()` y llaman a `.render(items, getCardProps)` sobre el root creado la primera vez.

### Dos bugs reales ya corregidos aquí — no los repitas en la próxima isla de React

1. **`mountSummaryCards()` vacía el contenedor a mano** (`containerEl.innerHTML = ''`) antes de `createRoot()`. `createRoot()` (React 18+) **no** borra los hijos preexistentes del contenedor al montarse — eso solo pasaba con la antigua `ReactDOM.render()` (React ≤17). Sin este vaciado explícito, el esqueleto de carga (`Skeleton.render()`, DOM imperativo pintado ANTES de que exista el root) se quedaba mezclado para siempre con las tarjetas reales.
2. **`initScrollReveal()` se dispara dentro de `SummaryCardGrid.jsx`, en un `useEffect`** — nunca justo después de llamar a `.render()` desde `index.js`/`nightsSection.js`. Un `root.render()` sobre un root **ya montado** es una actualización normal de React, no el mount inicial, y esas actualizaciones no se comprometen al DOM de forma síncrona. Un script clásico que llamara a `initScrollReveal()` en la línea siguiente podía correr antes de que las tarjetas nuevas existieran de verdad — su `querySelectorAll` no encontraba nada que observar, y esas tarjetas se quedaban con la clase `anim-slam`/`anim-fade-up` pero sin `is-visible`, en `opacity: 0` para siempre.

**Regla general para la próxima isla de React que se añada**: cualquier efecto que dependa de que el DOM ya refleje el último render (medirlo, leerlo, engancharle un `IntersectionObserver`...) va DENTRO del componente (`useEffect`/`useLayoutEffect`), nunca en el script clásico que llama a `.render()` justo después. Es el mismo tipo de carrera que la regla de `DOMContentLoaded` del nav (ver más abajo) — misma solución: resolverlo dentro de React, no confiar en que un script externo acierte el timing.

### Reglas al tocar o ampliar la familia (salidas de la revisión final)

- **El anillo de foco es un `box-shadow`** (`:focus-visible` global en `src/styles/typography.css`, `outline: none`). Cualquier `.card--*` **enfocable** (un `<a>`) que ponga `box-shadow: none` (en reposo o en `:hover`) lo anula y deja la card sin indicador de foco por teclado — pasó con `tile` en la fase 3a y se corrigió en la revisión. Si una card nueva es un enlace, restablece `:focus-visible { box-shadow: var(--shadow-focus) }` después de sus reglas de hover (mismo peso de selector, gana la última). Ya lo hacen `.card--tile` y `.card--photo`.
- **Área táctil**: los CTA de texto (`info`, `link`) miden ~22–24px de alto; un `::after` invisible (`inset: -12px -8px`) amplía la zona pulsable a ≥44px sin cambiar el diseño. Un CTA de texto nuevo debe hacer lo mismo.
- **Contraste conocido en Parties (no de la familia, de los tokens)**: con `?exp=parties`, el color primario/categoría sobre superficie da ~4.0:1 (fecha del evento, CTA `info`/`link`) y ~3.6:1 (etiqueta de categoría de las cards de ciudad) — por debajo de 4.5:1 para texto pequeño. Es el mismo color que ya usaban esos elementos antes de la migración; se arregla en los tokens del tema, no en `Card`. En Verified no hay ningún fallo.
- **El anillo de foco global es de baja opacidad** (`rgba(…, 0.25)`, ~1.4:1 sobre la superficie): no llega a 3:1 (WCAG 1.4.11). También es anterior a la familia y afecta a toda la web.
- **`prefers-reduced-motion`** solo acorta las transiciones a ~0 (regla global); el desplazamiento de hover (`translateY(-5px)`) sigue aplicándose, sin animar.
- **Contenedores de grid, mismo criterio que `.card--<layout>`**: cada `<div>` que agrupa cards de la familia lleva `class="card-grid card-grid--<layout>"` — `card-grid--service` (servicios, + `card-grid--service-2col` en `servicios.html`), `card-grid--stacked` (partners/eventos del home) y `card-grid--photo` (ciudades). `.card-grid` (en `card.css`) solo pone `display: grid`; `grid-template-columns`/`gap`/breakpoints de cada uno siguen en el CSS de su página, como antes de unificar el nombre. Antes cada grid tenía un nombre suelto sin patrón común (`.collab-grid`, `.services-grid`, `.partner-grid`, `.cities-grid`). `card-grid--tile` no tiene ya ningún consumidor (mismo motivo que `layout="tile"` en `Card.jsx`) — no se ha borrado el CSS de `.card--tile`, solo el grid dedicado. Excepción a propósito: `.city-items-grid` (`ciudades-todas.html`) no entra en este esquema — es una estrategia de columnas distinta (auto-fill por letra) ya documentada como propia de esa página, ver [Estructura de `src/css/`](#estructura-de-srccss).

## Lista de partners (PartnerCategoryList)

La cabecera de categoría (pastilla de activar/desactivar, o heading simple si la ciudad solo tiene una categoría con partners) + la lista de partners de esa categoría, en el aside de `ciudad.html`/`mapa.html`, ya no se construyen a mano con DOM imperativo (`buildGroupSection()`) — usan un componente React compartido. Es la **tercera isla de React** del proyecto (ver [Familia de cards](#familia-de-cards-card) para la segunda) y la primera con foto de contenido real (miniatura de partner, 28px) en la propia fila, no solo en la tarjeta de resumen.

### Qué hace cada archivo

| Archivo                             | Qué hace                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/react/PartnerCategoryList.jsx` | Componente presentacional (sin fetch ni estado propio, como `Card`). Recibe `{ groups, activeCategories, onToggleCategory, onSelectPartner }` — cada grupo llega con `label`/`icon`/`color` **ya resueltos**, porque `CATEGORY_META` (`map-helpers.js`) es un `const` de script clásico y no vive en `window`: un módulo ES no puede leerlo como identificador suelto. `mapPartners.js` hace esa traducción de dominio una sola vez (`listGroups`), igual que `getPartnerCardProps()`/`getEventCardProps()` para `SummaryCard`. Sanea `partner.image_url` con `sanitizeUrl()` antes de usarlo en `src`; si no hay imagen válida, no renderiza la etiqueta `<img>` en absoluto — sin hueco ni icono roto. |
| `src/react/mount-partner-list.jsx`  | Expone `window.mountPartnerCategoryList(containerEl)` — mismo patrón que `mount-summary-cards.jsx`: vacía el contenedor antes de `createRoot()` y devuelve `{ render(groups, activeCategories, onToggleCategory, onSelectPartner) }` para que `mapPartners.js` repinte sobre ESE MISMO root en cada `toggleCategory()` y en el flujo de deep-link `?partner=`.                                                                                                                                                                                                                                                                                                                                           |

`renderList()` (`mapPartners.js`) crea el root una única vez (`if (!categoryListRoot) categoryListRoot = mountPartnerCategoryList(listReactHost)`) y llama a `.render(listGroups, state.activeCategories, toggleCategory, selectPartner)` sobre él en cada cambio.

### Tercer caso real de "un root de React necesita su propio contenedor dedicado" — no lo repitas en la próxima isla

Los dos casos ya documentados arriba (`mountSummaryCards()` vaciando su contenedor a mano; `initScrollReveal()` dentro de un `useEffect`) son sobre _cuándo_ React toca el DOM. Este es sobre _qué otro contenido_ puede — o no — convivir en el mismo contenedor que un root de React:

`renderList()` necesitaba seguir mostrando un `<p class="partners-count">` (texto de recuento, solo cuando la ciudad tiene una única categoría con partners — sin control de activar/desactivar, nada que filtrar) **antes** de la lista, como DOM plano fuera de React — ese texto no forma parte de los props que recibe `PartnerCategoryList.jsx`. El primer intento fue montar el root directamente sobre el aside completo (`container`, el mismo que recibía `Skeleton.render()`), igual que hace `mountSummaryCards(grid)` sobre `#partnerGrid`. Eso rompía el texto de recuento: un root de React asume propiedad **exclusiva** de todo lo que hay dentro de su contenedor, así que cualquier nodo que no gestione él (como ese `<p>` añadido a mano) queda en una posición indefinida en cuanto React vuelve a renderizar.

La solución: `mountPartnerCategoryList()` se monta sobre un `<div>` propio (`listReactHost`), creado una vez como hijo de `container` — nunca sobre `container` directamente. El `<p class="partners-count">` sigue viviendo como hermano DOM plano de ese `<div>`, insertado siempre con `container.insertBefore(buildCountText(...), listReactHost)` para conservar el mismo orden visual que tenía antes de la migración (el texto de recuento arriba, la lista debajo) — verificado con Playwright: el `<p>` aparece por delante del primer `.partner-group` tanto en el DOM (`compareDocumentPosition`) como en pantalla (`getBoundingClientRect().top`).

**Regla**: si una isla de React necesita compartir un contenedor con DOM que NO gestiona (un texto, un botón suelto, un `Skeleton.render()` previo que hay que limpiar primero), no montes el root directamente sobre ese contenedor — créale un `<div>` hijo dedicado y usa `insertBefore`/`appendChild` alrededor de él para el resto del contenido. Montar el root sobre un contenedor compartido solo funciona cuando React es dueño de TODO lo que hay dentro (como `#partnerGrid`, que `mountSummaryCards()` sí puede vaciar entero sin perder nada).

## Detalle de partner (PartnerDetail)

El contenido del Sheet que se abre al pulsar un partner en `ciudad.html`/`mapa.html` (descripción + enlaces + botón "Cómo llegar") ya no se construye a mano con DOM imperativo (`buildPartnerDetail()`) — usa un componente React. Es la **cuarta isla de React** del proyecto y la primera con un root **efímero**: a diferencia de las otras tres (montadas una vez y reutilizadas toda la vida de la página), aquí se crea un root nuevo en cada apertura y se desmonta explícitamente al cerrar. `sheet.js` (el `<dialog>` en sí — focus trap, drag-to-close, `<dialog>` nativo) sigue siendo vanilla a propósito: ese archivo tiene su propio comentario de cabecera rechazando convertirse en isla de React, y esa decisión sigue vigente.

### Qué hace cada archivo

| Archivo                              | Qué hace                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/react/PartnerDetail.jsx`        | Componente presentacional (sin fetch ni estado propio). Recibe `{ partner, directionsLabel, onLinkClick, onDirectionsClick }` — `partner.description` y cada `link.label` llegan **ya traducidos**: `fetchPartnersByCity()` (`partnersService.js`) los resuelve con `I18n.tField()` antes de que `mapPartners.js` los vea, así que el componente los usa directamente, sin llamar a `I18n`. Sanea cada `link.url` con `sanitizeUrl()` **dentro del propio componente** (mismo patrón que `sanitizeUrl(image_url)` en `PartnerCategoryList.jsx`); si no es válida, ese link no se renderiza. No llama a `trackEvent()` — delega en `onLinkClick`/`onDirectionsClick`, igual que `Card` delega `cta.onClick`. Sin `id` en los links (`fetchPartnersByCity()` no lo incluye): usa `link.url` como key. |
| `src/react/mount-partner-detail.jsx` | Expone `window.mountPartnerDetail(containerEl, props)`. A diferencia de `mount-summary-cards.jsx`/`mount-partner-list.jsx`, no hay `.render()` reutilizable ni falta vaciar `containerEl` antes de `createRoot()` (el contenedor es un `<div>` recién creado en cada `selectPartner()`, nunca reutilizado). Devuelve `{ unmount() }`.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

### Cuarto caso real: cuándo un root de React debe ser efímero, no reutilizado

Los tres casos ya documentados arriba (vaciar el contenedor a mano; efectos dentro de `useEffect`; un `<div>` dedicado para compartir contenedor con DOM ajeno) asumen todos un contenedor que vive **toda la vida de la página** — el patrón por defecto de este proyecto (`mountSummaryCards()`, `mountPartnerCategoryList()`): crear el root una vez, reutilizarlo con `.render()` en cada actualización.

`Sheet.create()` (`sheet.js`) rompe esa asunción: crea un `<dialog>` **nuevo** cada vez que se abre un partner y no lo elimina del DOM al cerrarse, solo lo oculta (deuda ya existente en `sheet.js`, no introducida por esta isla). Reutilizar el patrón de root único aquí habría significado, o bien crear un root nuevo sin desmontar el anterior (cada partner visto en una sesión — típicamente varios seguidos en móvil — deja un árbol de fibra de React huérfano en memoria), o intentar mantener un único root "flotante" reasignado entre `<dialog>` distintos, que es justo el tipo de gestión manual de ciclo de vida que React ya resuelve mejor con `unmount()`.

La solución: `mountPartnerDetail()` crea un root nuevo en cada `selectPartner()` y devuelve `{ unmount() }`; `mapPartners.js` llama a `detailRoot.unmount()` explícitamente en el `onClose` del propio Sheet, junto a la limpieza que ya hacía (`setMarkerExpanded` a `false`). Verificado con Playwright envolviendo `mountPartnerDetail()` para contar mounts/unmounts a lo largo de varias aperturas: cada mount tiene su unmount, y el `<div>` que queda dentro del `<dialog>` huérfano se confirma vacío tras cerrar (sin contenido de React colgando, solo el nodo contenedor).

**Regla**: si el contenedor de una isla de React no vive toda la vida de la página (se crea de nuevo en cada uso, como un modal/Sheet/tooltip efímero), el root tampoco debe reutilizarse — créalo en el momento de montar y desmóntalo explícitamente (`root.unmount()`) en el evento de cierre/limpieza correspondiente. El patrón de root único de `mountSummaryCards()`/`mountPartnerCategoryList()` es correcto solo para contenedores persistentes.

## Ficha individual (ListingDetail)

`alojamiento.html`/`viaje.html` (singular — ver la nota de nomenclatura en [Páginas y sus scripts](#páginas-y-sus-scripts)) son la ficha de **un** alojamiento o **un** viaje en concreto, diseño unificado para los dos (un mockup validado explícitamente por Álvaro antes de implementar, vía un canvas de diseño). No confundir con `alojamientos.html`/`viajes.html` (el listado de TODOS, ya existente).

### Qué hace cada archivo

| Archivo                                                         | Qué hace                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/react/ListingDetail.jsx`                                   | Componente único para las dos páginas — prop `kind` (`'alojamiento'` \| `'viaje'`) decide vocabulario (breadcrumb, icono de meta, título de sección) y qué set de `listingsData.js` leer. El id activo se lee de `?id=` con `new URLSearchParams(window.location.search)` **directamente en el componente** (mismo criterio que `Nav.jsx` leyendo `window.ERASMUS_EXPERIENCE`, sin `DOMContentLoaded`, ver la regla de arquitectura en [Navegación](#navegación)) porque esta isla no tiene script clásico propio que se lo resuelva antes. **Sin distinción de terceros** (decisión explícita de Álvaro, ver más abajo): toda ficha muestra badge "Reserva directa", meta "Organizado por Erasmus Verified" y precio "Gestionado directamente por Erasmus Verified" — el `linkType` `'externo'`/`'interno'` que bifurcaba esto (y el CTA "Ver oferta" a una web externa) ya no existe. El CTA es siempre "Solicitar información" + "Ver disponibilidad", **sin acción real todavía** (pendiente de decidir con Álvaro qué pasa al pulsarlos — formulario, WhatsApp... de momento solo visual). Sin `?id=` en absoluto se muestra la primera ficha del `kind` como entrada de demostración; con `?id=` presente pero que no casa con ninguna, un estado "no encontrado" propio (nunca se sustituye por la primera ficha en silencio — eso engañaría al usuario). Pone `document.title` en un `useEffect` (sin script clásico que lo haga por fuera). La sección "más similares" reutiliza `.events-scroll` (mismo carrusel móvil / rejilla escritorio que "Trending nights" y que `alojamientos.html`/`viajes.html`) con markup calcado al que produce `Card.jsx` en `layout` por defecto (`.card > .card__media > .card-img-placeholder`, `.card__body`), para heredar sus estilos sin pasar por el componente.                                                                                                                                                       |
| `src/react/listingsData.js`                                     | **Provisional**: array en memoria (`LISTINGS.alojamiento`/`LISTINGS.viaje`, **10 entradas cada uno**) — sustituye a una tabla de Supabase que todavía no existe. Acordado explícitamente con Álvaro: la tabla nueva es el plan a medio plazo, esto es solo para poder navegar/iterar el diseño real antes de esa migración. Datos de desarrollador, no hace falta `escapeHtml()`. Sin `linkType`/`source`/`ctaHref` (retirados con la distinción de terceros, ver más abajo): cada entrada es solo `{ id, title, location, price, priceUnit, description, features, photoCount }`. Los alojamientos son pisos/habitaciones/residencias por barrio de Barcelona; los viajes pasaron de "compara FlixBus/BlaBlaCar/Omio" a **excursiones organizadas por Erasmus Verified** (Costa Brava, Andorra, Pirineos, Ibiza, Girona-Figueres, Priorat, Zaragoza, San Sebastián, Montserrat, Sevilla) — un proveedor de transporte de terceros no puede ser "gestionado por nosotros", así que el modelo de "viaje" pasó de billete a excursión. Cada entrada lleva `photoCount` (4-11, varía a propósito entre fichas) — cuántas fotos "tendría", aunque hoy todas sean el mismo placeholder liso: alimenta la galería y el lightbox sin esperar a tener URLs reales.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/react/mount-listing-detail.jsx`                            | Auto-monta `<ListingDetail kind={...} />` sobre `<div id="listing-detail-root" data-listing-kind="alojamiento\|viaje">` — una única vez, sin `.render()` reutilizable (cada navegación con `?id=` distinto recarga la página entera, no hay SPA routing).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/react/ListingGalleryLightbox.jsx`                          | Visor de fotos a pantalla completa — se abre al pulsar la foto principal, la miniatura o el "+N" de la galería (`ListingDetail.jsx` guarda el índice activo en `useState`; `null` = cerrado). `<dialog>` nativo con `showModal()`/`close()` en un `useEffect` (mismo motivo que `sheet.js`: focus trap y aislamiento del resto de la página gratis del propio elemento), pero en **React** — a diferencia de `sheet.js`, que es vanilla a propósito porque vive en páginas con script clásico de orquestación; `ListingDetail.jsx` no tiene ninguno (la página entera es la isla), así que aquí el patrón natural es un componente más. Cierra con el botón X, con Escape (evento `cancel` nativo del `<dialog>`) o al pulsar el fondo (`onClick` en el propio `<dialog>`, comprobando `event.target === el propio dialog` — un clic dentro de la foto o los botones nunca burbujea con ese target). Flechas prev/siguiente con wrap-around; sin flechas ni contador si `photoCount` es 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/css/pages/listing-detail.css`                              | Único archivo para las dos páginas (`body.listing-detail-page`, mismo degradado que `alojamientos.html`/`servicios.html`). Mobile-first: galería de una sola foto grande en móvil, 2fr/1fr con miniaturas desde `--bp-md`; `.listing-layout` apilado en móvil, `1fr 360px` con sidebar `position: sticky` desde `--bp-md`; `.listing-features-grid` 1 columna → 2 desde `--bp-md`. `.listing-gallery-main`/`.listing-gallery-thumb` son `<button>` (no `<div>`): reset de estilos de botón nativo para seguir pareciendo la misma foto de siempre. `.listing-lightbox*` es el visor de fotos — `::backdrop` semitransparente (`rgba(10,10,15,0.92)`, se ve tenuemente la página detrás a propósito) en vez de negro sólido.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/react/FeaturedListings.jsx`, `mount-featured-listings.jsx` | **Única sección** de `alojamientos.html`/`viajes.html` — ya no hay una segunda sección de "Plataformas colaboradoras" (`CollabGrid.jsx`, retirado del proyecto por completo, ver más abajo). Una `Card` (`layout` por defecto, CTA `offer` — botón relleno) por cada entrada de `LISTINGS[kind]` (`listingsData.js`) que pase los filtros, enlazando a `alojamiento.html?id=ID`/`viaje.html?id=ID`. Dos filtros en memoria (`useState`, sin red — 10 entradas hoy), alineados a la izquierda como un único grupo (`.featured-filter`, mismo `max-width`/`margin`/`padding` que `.nights-header`/`.events-scroll` en `home.css` — y la misma regla de `responsive.css` a `--bp-lg` que amplía ese padding a `--margin-desktop` — para que la barra empiece exactamente donde empieza el título y la primera card, sin ser hijo de `.section`): (1) texto libre — `normalize()` quita acentos/mayúsculas (mismo criterio que `ciudades-todas.html`) sobre `título + location + description`, no solo el título — el placeholder promete "nombre o zona"/"destino", y "Barcelona" no aparece en el título de "Estudio en Gràcia" pero sí en su `location`; (2) orden por precio (`<select>`, `priceValue()` parsea el primer número de `item.price` — "450 €" → `450` — para poder comparar, ya que el campo es texto libre pensado para mostrarse tal cual). Botón de limpiar (solo el texto libre) y un estado vacío propio (`.featured-filter-empty`) si ningún filtro casa — sin contador "N / total", quitado a petición explícita, y sin filtro de tipo interno/externo, quitado junto con la distinción de terceros (ver más abajo). El root de React incluye la barra de filtros Y la rejilla de cards en el mismo árbol porque comparten estado — por eso el `<div data-featured-listings="alojamiento\|viaje">` en el HTML ya NO lleva la clase `events-scroll`: `FeaturedListings.jsx` la pinta ella misma alrededor de las cards, dentro de su propio render. |

Páginas: mismo boilerplate exacto que `alojamientos.html`/`viajes.html` (tokens, tipografía, `experience.js` con el mismo redirect a `index.html` en Parties, nav/footer/bottom-nav, `sanitize.js` — necesario porque `ListingDetail.jsx` llama a `window.sanitizeUrl()`, aunque los datos de hoy sean estáticos) — sin `citiesService.js`/`partnersService.js`/`tracking.js`, que no aplican todavía sin Supabase real.

**Pendiente, no bloquea lo ya construido**: tabla real en Supabase (sustituye a `listingsData.js`, incluidas las fotos reales del lightbox), y decidir la acción del CTA "interno" (formulario propio, WhatsApp, u otra cosa).

### Sin distinción de terceros — todo gestionado por Erasmus Verified

Decisión explícita de Álvaro, un cambio de rumbo respecto al diseño anterior: `alojamientos.html`/`viajes.html` dejaron de distinguir "empresas grandes que redirigen fuera" de "alojamiento/viaje que gestionamos nosotros" — a partir de ahora **todo** alojamiento y **todo** viaje está gestionado directamente por Erasmus Verified, sin excepción. Esto se retiró por completo, no solo se ocultó:

- **`CollabGrid.jsx`, `collabData.js`, `mount-collab-grid.jsx` — borrados del repo.** Era la sección "Plataformas colaboradoras" (segunda sección de `alojamientos.html`/`viajes.html`, con Uniplaces/Spotahome/HousingAnywhere/Badi/FlixBus/BlaBlaCar/Omio/Rentalcars) y la sección "Nuestros colaboradores" del home (`index.html`, `#alojamiento`, con el mismo grid `housing`) — las tres han desaparecido, no solo la de las páginas dedicadas: mantener la del home habría dejado un enlace a una sección que ya no existe en `alojamientos.html`.
- **`Card.jsx`**: el CTA `external` desapareció de `CTA_KINDS` (era exclusivo de `CollabGrid.jsx`). `.btn-outline-pill` (`components.css`) y `.card--external`/`.card-cta--external` (`card.css`) se borraron con él — no tenían ya ningún otro consumidor.
- **`ListingDetail.jsx`**: se retiró el `linkType` `'externo'`/`'interno'` de `listingsData.js` — toda ficha es ahora la misma: badge "Reserva directa" (`.listing-badge--external` borrado de `listing-detail.css`), meta "Organizado por Erasmus Verified" y precio "Gestionado directamente por Erasmus Verified", con el mismo CTA "Solicitar información" + "Ver disponibilidad" que antes solo veían las fichas internas.
- **`FeaturedListings.jsx`**: sin el filtro de tipo (interno/externo) que había tenido brevemente — ya no hay dos tipos que filtrar.
- El bloque "¿Eres una empresa de alojamiento/transporte? Colabora con nosotros" (`.collab-contact`, al final de ambas páginas) también se quitó — invitar a terceros a redirigir tráfico fuera de la web contradice la propia decisión.
- Traducciones retiradas (`translations.js`, es/en): `common.collaborate_with_us`/`contact_cta`, `home.accommodation_eyebrow`/`collaborators_title`/`collaborators_subtitle`/`collab_*_desc`/`view_all_collaborators`, `accommodation.company_eyebrow`/`collab_body`, `trips.collab_*_desc`/`company_eyebrow`/`collab_body`, `listing.external_badge`/`via_prefix`/`opens_on_prefix`/`collabs_*`/`filter_type_*`.

Si algún día una propiedad o un proveedor de transporte concreto necesitara redirigir a su propia web de verdad, habría que reintroducir ese concepto desde cero (no queda ningún resto de `linkType`/CTA `external` sobre el que apoyarse) — decisión a tomar con Álvaro si llega el caso, no algo que se dé por hecho.

## Página de error 404 (`404.html`)

`404.html` es una página real de error HTTP, no solo un enlace más del menú — se activa de verdad cuando el visitante llega a una URL que no existe, con **status 404 real**, no un simple contenido que "parece" un 404 mientras el servidor sigue devolviendo 200. Reutiliza el mismo boilerplate que el resto de páginas públicas (tokens, tipografía, `experience.js`, Nav/Footer/bottom-nav React) — comparte esa infraestructura pero, igual que `admin/index.html`, **no** es una de "las 13 páginas públicas" enumeradas en el resto de este documento: no está enlazada desde ningún sitio, solo se llega por accidente (URL mal escrita, enlace roto, etc.).

- **Cómo se activa de verdad**: `vite.config.js` declara `appType: 'mpa'` (en vez del `'spa'` por defecto de Vite) — sin esto, tanto `npm run dev` como `npm run preview` hacían _fallback_ silencioso a `index.html` con status 200 para cualquier ruta que no existiera (bug real preexistente, no específico de esta página: cualquier URL mal escrita ya se comía en local sin avisar). `404.html` se añade además como entrada más de `rollupOptions.input` (como cualquier otra página) para que `npm run build` la compile a `dist/404.html` con las globals de Supabase inyectadas igual que las demás. En producción (Vercel, sin `vercel.json` en el repo) esto ya es suficiente: Vercel sirve `dist/404.html` automáticamente con status 404 de verdad para cualquier ruta sin archivo — comportamiento por defecto de su output estático, no hace falta configurar rutas a mano.
- **Verificación en local**: un segundo plugin en `vite.config.js` (`serve-built-404`) hace que `npm run preview` sirva el `dist/404.html` ya compilado con status 404 real para cualquier ruta sin archivo — el mismo comportamiento que Vercel en producción, para poder revisar la página antes de desplegar (verificado con `curl -w "%{http_code}"` y Playwright: status 404 confirmado, cuerpo = la página real). Solo aplica a `npm run preview` (opera sobre `dist/`, ya compilado); en `npm run dev` sigue devolviendo un 404 genérico sin cuerpo propio, porque ahí `404.html` todavía no ha pasado por `transformIndexHtml`.
- **Diseño — "este chat ya no existe"**: en vez del típico "404 — página no encontrada" plano, la página se presenta como una conversación de WhatsApp donde "esta página" ha salido del grupo — coherente con que el propio producto es, literalmente, un directorio de grupos de WhatsApp/Telegram. `--wa` (`base.css`, el mismo verde que ya usa el botón real de WhatsApp en `ciudad.css`) tiñe la burbuja de salida a propósito, no es un color nuevo. La única aparición del número "404" en toda la página es la insignia de "no leídos" sobre el avatar del chat — deliberadamente NO hay un "404" gigante de fondo, la solución por defecto de cualquier página de error genérica.
- **La ruta real intentada se muestra dentro del chat**: `src/js/notFound.js` lee `window.location.pathname + search` y lo inserta con `textContent` (nunca `innerHTML`) en la burbuja entrante — es la única página de la web que muestra en pantalla un dato que viene directo de la barra de direcciones del propio visitante sobre sí mismo.
- **Animación orquestada, no decorativa suelta**: las dos burbujas entran con `animation-delay` escalonado (CSS puro, `not-found.css`); `notFound.js` solo orquesta el paso siguiente — a los 1400ms aparece el indicador de "escribiendo…" y a los 2600ms se sustituye por el mensaje de sistema "Esta página salió del grupo" (mismo patrón visual que un mensaje de sistema real de WhatsApp: píldora gris centrada, sin cola de burbuja). Respeta `prefers-reduced-motion`: sin animación, el mensaje de sistema aparece directo, sin esperar (verificado con Playwright, `emulateMedia({ reducedMotion: 'reduce' })`).
- **Reacciona a la experiencia Parties igual que cualquier otra página**: sin código especial — `experience.js` ya retematiza Nav/Footer/bottom-nav como en el resto del sitio, y como el chat usa tokens (`--surface`, `--primary`, `--secondary`, `--on-surface*`) en vez de colores fijos, se adapta solo al tema oscuro/magenta de Parties (verificado visualmente con `?exp=parties`); `--wa` es la única excepción intencional, fijo en los dos temas porque la broma depende de que se lea como el verde real de WhatsApp.
- **`.notfound-chip`** (botones "Ver ciudades"/"Abrir el mapa"): deliberadamente NO son miembros de la familia `.btn-primary-pill`/`.btn-ghost-pill` (`components.css`) — son "respuestas rápidas" secundarias a "Ir al inicio" (que sí es un `.btn-primary-pill`), con su propia talla más pequeña, mismo criterio que un chat real ofrece respuestas rápidas más discretas que el mensaje principal.
- **i18n**: sección `notfound` en `translations.js` (es/en), resuelta con `data-i18n` + `applyTranslations()` como el resto de contenido estático de página — no es una isla de React, así que no aplica la regla de `t()`/`navShared.jsx`.

## Footer (Footer.jsx)

El `<footer>` compartido de las 13 páginas públicas ya no es HTML duplicado byte a byte en cada una — vive en `src/react/Footer.jsx`, otra isla de React, del mismo tipo "montada una vez, nunca vuelve a renderizarse" que `Nav.jsx`/`TopbarNav.jsx` (ver [Navegación](#navegación) para el porqué de esa regla — aplica igual aquí).

### Qué hace cada archivo

| Archivo                      | Qué hace                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/react/Footer.jsx`       | Componente único: marca/tagline, columna "Plataforma" (Destinos/Alojamiento/Fiestas/Viajes — Alojamiento y Viajes solo si `window.ERASMUS_EXPERIENCE.showAlojamiento`/`showViajes`, igual que hace `NavLinks` en `navShared.jsx`), columna "Legal" (enlaces `#`, sin páginas propias todavía) y el año de copyright (`new Date().getFullYear()`). Resuelve tema Parties/i18n él mismo al renderizar — mismas reglas que `Nav.jsx`/`TopbarNav.jsx`, nada de `DOMContentLoaded` (ver regla de arquitectura en [Navegación](#navegación)). Se marca con `data-react-footer="true"` en la raíz para que `experience.js` sepa que no debe tocarlo dos veces (mismo patrón que `data-react-nav`). |
| `src/react/mount-footer.jsx` | Expone el montaje sobre `<div id="footer-root">` — `createRoot(root).render(<Footer />)`, sin `.render()` reutilizable porque, como el nav, no necesita repintarse desde fuera.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

`<div id="footer-root"></div>` seguido de `<script type="module" src="/src/react/mount-footer.jsx">` aparece en las **13 páginas públicas**, `mapa.html` incluida — el comentario de cabecera de `Footer.jsx` todavía dice "todas salvo `mapa.html`" (una nota de una versión anterior a que se decidiera añadirlo también ahí); en el HTML real ya no hay excepción.

## Navegación

El menú de las 13 páginas públicas (todo salvo `/admin`) es **React** (`src/react/Nav.jsx` / `TopbarNav.jsx`, ver más abajo) — antes era HTML duplicado byte a byte en cada página, ahora vive en un único sitio. Sigue habiendo tres estilos visuales de header según la página (heredados del diseño previo a la migración), pero los tres los renderiza el mismo par de componentes.

### Patrones de header y qué los monta

| Patrón CSS             | Páginas                                                                                                                           | Componente                                   | Script de montaje           |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | --------------------------- |
| `.topnav`              | `index.html`, `ciudades-todas.html`                                                                                               | `Nav.jsx`                                    | `mount-nav.jsx`             |
| `header.topbar`        | `ciudad.html`, `mapa.html`, `servicios.html`, `viajes.html`, `alojamientos.html`, `login.html`, `registro.html`, `recuperar.html` | `TopbarNav.jsx` (`as="header"`, por defecto) | `mount-topbar-nav.jsx`      |
| `.hero-legacy .topbar` | `ciudades.html`                                                                                                                   | `TopbarNav.jsx` (`as="div"`)                 | `mount-hero-legacy-nav.jsx` |

Cada página tiene un `<div id="nav-root"></div>` seguido de `<script type="module" src="/src/react/mount-*.jsx">` en el sitio donde antes iba el header estático — Vite descubre esos scripts automáticamente por estar referenciados desde un HTML ya registrado en `vite.config.js`, no hace falta añadirlos a mano. `Nav.jsx`/`TopbarNav.jsx` incluyen además el icono de cuenta (`#authBtn`, `AuthButton()` en `navShared.jsx` — funcional de verdad: enlaza a `login.html` sin sesión, desplegable con "Cerrar sesión" con sesión, ver [Autenticación de usuarios](#autenticación-de-usuarios-loginhtml--registrohtml--recuperarhtml)) y `TopbarNav.jsx` acepta un prop `backLink` opcional:

- `mapa.html`: lleva botón de "volver" (a la ciudad), configurado justo antes del `<script type="module">` con una línea `window.__BACK_LINK__ = { i18nKey, label, href }` — su propio script (`mapa.js`) sobreescribe `href`/texto tras cargar datos de Supabase. `ciudad.html` ya no lo lleva (se quitó el botón "Inicio" que volvía al home).
- `ciudades.html`: el back-link va hardcodeado en `mount-hero-legacy-nav.jsx` (siempre "Todos los países" → `index.html`, nunca cambia).
- El resto de páginas del patrón `header.topbar` no pasan `backLink` y `TopbarNav.jsx` no lo renderiza.

Visualmente los tres patrones están **unificados**: mismo truco de grid de 3 columnas (`1fr auto 1fr`) que centra los links en todo el ancho de la barra, mismo icono de cuenta, y el mismo subrayado degradado en hover/foco (antes solo existía en `index.html`, escapado bajo `body.home-page`; ahora vive bajo los selectores `.topnav`/`.topbar` directamente en `src/css/layout.css`, así que aplica a las 13 páginas).

`.mobile-nav` (el overlay del menú móvil) y el toggle de la hamburguesa también los renderiza React (`MobileNavOverlay` en `navShared.jsx`) — no queda ningún bloque `<div class="mobile-nav">` estático en el HTML. El `.hamburger-btn` (`id="hamburgerBtn"`) es `display: none` en TODOS los anchos, no solo en móvil — sin ningún `@media` que lo reactive en desktop (confirmado con grep y con el propio comentario de `components.css`: "`.hamburger-btn` no tiene ningún `display:flex` en ninguna parte del CSS"). React lo sigue montando, pero hoy es un botón inerte en cualquier ancho — la navegación desktop la resuelve `.topbar-nav`/`.topnav-links` (visibles desde `--bp-md`), nunca el hamburguesa; en móvil la gestiona el bottom-nav.

### Regla de arquitectura: nada de `DOMContentLoaded` para tocar el nav

Todo lo que antes hacían scripts externos "buscando" nodos del nav después de que la página cargara (sombra de scroll al hacer scroll, aplicar el tema Parties, año/idioma del lang-switcher) se movió **dentro** de los componentes React, en su primer render o en un `useEffect`. Motivo, comprobado en la práctica y no solo en teoría: un script clásico enganchado a `DOMContentLoaded` que hace `document.getElementById('topNav')` puede disparar **antes** de que React haya terminado de montar (el commit real al DOM de `createRoot().render()` lo encola el scheduler de React, y en dev con Vite eso puede tardar más que el resto del parseo) — se comprobó con una sombra de scroll que fallaba el 100% de las veces con este patrón.

Por eso:

- `Nav.jsx`/`TopbarNav.jsx` leen `window.ERASMUS_EXPERIENCE` (tema Parties) y `window.I18n` (idioma/traducciones) directamente al renderizar, en vez de depender de que `experience.js`/`applyTranslations()` los mute después.
- Las mutaciones que SÍ siguen en `experience.js` (porque también afectan a HTML estático fuera del nav, como el footer) llevan un guard `if (el.closest('[data-react-nav]')) return;` para no tocar dos veces lo que el componente React ya resolvió. `data-react-nav="true"` está en el `<nav>`/`<header>`/`<div>` raíz y en el overlay móvil de ambos componentes.
- `langSwitcher.js` tiene el mismo guard — el botón `#lang-switcher` dentro del nav lleva su propio `onClick` en React; `langSwitcher.js` solo actúa si el botón que encuentra **no** está dentro de `[data-react-nav]` (páginas fuera de esta migración, si las hubiera en el futuro).

**Si se añade algo nuevo que necesite tocar el nav desde fuera, no uses `DOMContentLoaded` + `querySelector` — o se resuelve dentro del componente React, o se acepta que puede fallar de forma intermitente.**

### Bottom-nav (móvil, `<768px`)

Clase `.app-bottom-nav` — `position: fixed; bottom: 0; height: 60px; z-index: 500`. **Ya no es HTML estático**: se migró a React (`src/react/AppShell.jsx` + `mount-shell.jsx`, rama `feature/mobile-app-shell`) — antes era `<nav class="app-bottom-nav">` duplicado byte a byte en las páginas públicas (con 2 variantes de markup, 4 ítems en `index.html` / 3 en el resto) más un `<script>` inline por página calculando el ítem activo; ahora vive en un único componente montado sobre `<div id="shell-root">` en las 13 páginas públicas (no en `/admin`) — `login.html`/`registro.html` incluidas, ver [Autenticación de usuarios](#autenticación-de-usuarios-loginhtml--registrohtml--recuperarhtml).

Mismo patrón que `Nav.jsx` (ver la regla de arquitectura en [Navegación](#navegación)): nada de `DOMContentLoaded` buscando `.app-bottom-nav` después — el componente resuelve tema Parties, idioma e ítem activo él mismo al renderizar, marcado con `data-react-nav="true"` para que `experience.js` no lo toque dos veces (ver [Experiencia dual](#experiencia-dual-verified--parties)).

**Contenido — ya no hay variante de 3 vs. 4 ítems ni ítem de Cuenta.** `AppShell.jsx` renderiza siempre **5 ítems**, iguales en las 13 páginas:

1. **Inicio/Noches** — `index.html`, icono `home` (`nightlife` en la experiencia Parties, con el label cambiado a "Noches").
2. **Mapa** — `mapa.html`, icono `map`.
3. **Servicios** — `servicios.html`, icono `storefront`. Es el único ítem con etiqueta corta (`nav.services_short`, "Serv.") por debajo de 360px de ancho — con 5 ítems a ~64px cada uno, es la única etiqueta que realmente aprieta; el resto ya define su propio fallback corto igual al largo, así que no cambian visualmente en ningún ancho (`.app-bottom-nav-label-short`, oculta por defecto y visible solo bajo `@media (max-width: 359px)` en `components.css`).
4. **Viajes** — `viajes.html`, icono `flight`.
5. **Salto de dominio (Fiestas ↔ Verified)** — **ya no es "Cuenta"** (`#authBtnMobile` no existe en el HTML actual, ni en el nav de escritorio hay ya nada equivalente ahí). Es un enlace externo (`target="_blank"`) que cambia según la experiencia activa: en Verified apunta a `erasmusparties.org` (icono `nightlife`, magenta permanente vía `.app-bottom-nav-item--parties`, clase reutilizada de la época estática); en Parties apunta a `erasmusverified.com` (icono `verified`, clase `.nav-verified`). Nunca lleva `.app-bottom-nav-item--active` (no es una pestaña real de la página).

El ítem activo se calcula con `currentPage()` (`navShared.jsx`, mismo helper que usa el nav de escritorio) comparado contra el `href` de cada ítem, y recibe `.app-bottom-nav-item--active` — ya no hay ningún `<script>` inline por página haciendo ese cálculo a mano.

En móvil el cuerpo tiene `padding-bottom: 68px` para compensar la barra.

## CSS

Sistema de diseño basado en Material Design 3 — nombres de rol (`--primary`, `--surface-container-high`, `--on-surface-variant`...) definidos directamente en `:root` de `base.css`, **sin** el prefijo `--md-` (ese prefijo no aparece en ningún sitio del repo — verificado con grep; si lo ves mencionado en otro sitio, es una descripción desactualizada). Variables clave:

- `--primary: #4648d4`, `--secondary: #a93349`
- `--topbar-h` — altura real del `header.topbar`, inyectada por JS en `src/js/ciudad.js`; usada por `top` del mapa sticky y `padding-top` del mobile-nav dropdown
- Tipografía: Syne (display, `--font-display`) + Inter (body, `--font-body`), cargadas desde Google Fonts
- Iconos: Material Symbols Outlined (CDN)
- Contenedor máximo: `1280px`, gutter `24px`

De los alias legacy que quedan en `base.css` (`--bg`, `--text`, `--text-muted`, `--accent`, `--accent-dim`, `--surface-2`, `--border`, `--radius`), ninguno tiene ya un solo consumidor en el CSS del repo (verificado con grep) — son código muerto sin limpiar, no "tokens en uso de páginas antiguas". Los dos alias del mismo bloque que sí se usan de verdad, `--wa`/`--tg` (colores de marca WhatsApp/Telegram), se consumen en `pages/ciudad.css`.

### Tokens del design system (`src/styles/`)

`src/styles/tokens.css` + `src/styles/typography.css` son una segunda capa de tokens/tipografía global, cargada en las 13 páginas públicas (no en `/admin`) justo ANTES de `styles.css` en el `<head>` — no sustituye a los tokens de `base.css`, convive con ellos. Los propios comentarios de `tokens.css` documentan la relación con el sistema "legacy" (`base.css`) tramo a tramo:

- **Alias de compatibilidad**: la mayoría de tokens de color (`--color-primary`, `--color-bg`, `--color-surface`, `--color-dark`, `--color-text`, `--color-text-muted`, `--color-border`...) no redefinen un valor nuevo — apuntan con `var()` al token equivalente que YA existía en `base.css` (`--color-primary: var(--primary)`, etc.), para que ambos sistemas usen siempre el mismo hex sin mantenerlo sincronizado a mano en dos sitios. Mismo criterio para radios (`--radius-sm/md/lg/full` no se redeclaran; solo `--radius-xl`/`--radius-2xl` son nuevos de verdad, y `--radius-xl` no tiene consumidor todavía) y para `--font-display`/`--font-body` (tampoco se redeclaran: mismo nombre, mismo valor, ya en `base.css`).
- **Tokens sin equivalente previo, genuinamente nuevos**: escala de espaciado `--space-1` … `--space-20` (base 8px), escala tipográfica `--text-display`/`--text-h1`/`--text-h2`/`--text-h3`/`--text-body`/`--text-small` + pesos/interlineados, colores semánticos `--color-success`/`--color-warning`/`--color-error`/`--color-info` (sin consumidor todavía), sombras `--shadow-sm/md/lg/focus`, duraciones `--duration-fast/base/slow` + `--ease-out`, y los breakpoints `--bp-sm` (600px) / `--bp-md` (900px) / `--bp-lg` (1200px) — mobile-first, siempre pensados para `min-width`. Los `@media` de CSS no pueden leer custom properties, así que en la práctica estos valores se repiten como literales (`900px`, `600px`...) en cada `@media` del proyecto; `--bp-md` sí se lee desde JS vía `getComputedStyle` en `isDesktopLayout()` (`src/js/ciudad.js`), para que el corte del gesto-gate del mapa no se desincronice del valor del token.
- **`--header-h`/`--nav-bottom-h`/`--nav-bottom-total`/`--tap-min`**: alturas reales de chrome fijo, para poder escribir `calc(100dvh - var(--header-h))` (ver `pages/mapa.css`) en vez de un literal suelto. Son los valores REALES actuales, no un ideal de diseño — cambiarlos es una decisión visual aparte, no de este sistema de tokens.
- `typography.css` aplica tipografía global a `body`/`h1`/`h2`/`h3` con estos tokens nuevos. Para `body`, `base.css` (mismo selector, misma especificidad, carga después) gana en la práctica en toda propiedad que ambos tocan (`font-size`, `line-height`...) — pero los headings sin una clase más específica encima SÍ quedan con el estilo de `typography.css`: es el caso, por ejemplo, del `<h1>` sin clase de `ciudades-todas.html` ("Todas las ciudades"), que toma tamaño/peso/color de aquí. En el resto de páginas cada `<h1>` ya lleva su propia clase (`.city-name`, `.hero-title`, `.section-title`...) que sobreescribe estos valores genéricos. `.heading-display`/`.overline` (y el resto de selectores `[class*="overline"]`) no tienen todavía ningún consumidor en el HTML actual.

### Estructura de `src/css/`

`styles.css` ya no contiene reglas propias — es solo un manifiesto de `@import` que las 13 páginas públicas siguen cargando con el mismo `<link rel="stylesheet" href="/src/css/styles.css">` de siempre (no hubo que tocar ningún HTML). Vite resuelve esos `@import` tanto en dev (cada archivo se sirve y recarga por separado) como en build (los une en un único CSS por página, igual que cuando era un solo fichero — verificado que el CSS final compilado es byte a byte idéntico al de antes de la separación).

El **orden** de los `@import` en `styles.css` importa: es exactamente el orden en el que vivían estas secciones dentro del monolito original. No lo reordenes sin comprobar antes que ninguna regla dependa de la cascada entre archivos.

| Archivo                       | Contenido                                                                                                                                                                                                                                                         |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `base.css`                    | `:root` tokens (nombres de rol de Material Design 3, sin prefijo — ver [Tokens del design system](#tokens-del-design-system-srcstyles)), temas `.theme-verified`/`.theme-parties`, reset, utilidades, tipografía base                                             |
| `components.css`              | Buttons, badges, cards, forms/inputs, bottom-nav, Sheet, Skeleton                                                                                                                                                                                                 |
| `card.css`                    | Familia de cards (`.card`, `.card__*`, `.card-cta--*`) y la base compartida de sus contenedores (`.card-grid`, `.card-grid--<layout>`) — ver [Familia de cards](#familia-de-cards-card)                                                                           |
| `layout.css`                  | Top nav/header (`.topnav`, `.topbar` — el HTML lo renderiza React, ver [Navegación](#navegación), pero las clases y este archivo no cambiaron), footer, hero, section wrappers                                                                                    |
| `pages/home.css`              | Accordion grid de ciudades, nights section, services section, CTA (`index.html`)                                                                                                                                                                                  |
| `pages/ciudades-todas.css`    | `.all-cities-hero` — el resto de estilos de esa página (`.filter-bar`, `.city-items-grid`...) siguen en el `<style>` inline de `ciudades-todas.html`                                                                                                              |
| `pages/ciudad.css`            | Layout de ciudad (`.city-page`, header, buscador local) y la lista de partners sin mapa (`.city-partners-*`: grupos, foco, "ver todo"); el diseño de cada card vive en `card.css`                                                                                 |
| `pages/mapa.css`              | `.map-page-main`, `.map-canvas`, `.erasmus-pin__dot`, map-with-list                                                                                                                                                                                               |
| `pages/servicios.css`         | `body.servicios-page` (gradiente), `.servicios-hero` (cabecera centrada), `.servicios-category`, `.card-grid--service-2col`                                                                                                                                       |
| `pages/alojamientos.css`      | Estilos propios de `alojamientos.html`                                                                                                                                                                                                                            |
| `pages/listing-detail.css`    | Ficha individual — `alojamiento.html`/`viaje.html` (galería, badges, sidebar de precio, características, "más similares"), ver [Ficha individual](#ficha-individual-listingdetail)                                                                                |
| `pages/featured-listings.css` | Filtro de texto de `FeaturedListings.jsx` en `alojamientos.html`/`viajes.html` (`.featured-filter*`) — autónomo, no reutiliza `.search-bar` (pensada para el hero, con hueco reservado para `.search-bar-btn` flotante)                                           |
| `pages/auth.css`              | Tarjeta y formularios de `login.html`/`registro.html`/`recuperar.html` (`.auth-card--glass`, `.auth-switcher`, `.auth-step-*`…, ver [Diseño "Living Card"](#diseño-living-card--selector-y-registro-en-pasos)), incluido el autocompletado de ciudad del registro |
| `pages/not-found.css`         | `404.html` (`.notfound-*`) — tarjeta de chat, burbujas, indicador de "escribiendo…", mensaje de sistema y chips de respuesta rápida, ver [Página de error 404](#página-de-error-404-404html)                                                                      |
| `responsive.css`              | Media queries globales que afectan a múltiples archivos                                                                                                                                                                                                           |
| `transitions.css`             | View Transitions entre páginas                                                                                                                                                                                                                                    |

El panel de administración tiene su propio archivo separado, `src/css/admin.css` (no se toca en este split), y `animations.css` (scroll-reveal) también sigue siendo su propio archivo — ninguno de los dos pasa por `styles.css`.

`.hamburger-btn` no tiene ningún `display: flex` en ninguna parte del CSS — es `display: none` en todos los anchos (la navegación móvil la gestiona el bottom-nav, ver [Navegación](#navegación)); si algún día se reactiva el menú hamburguesa, vigilar el orden entre `components.css` (bottom-nav) y `layout.css` (top nav/header), que es donde antes vivía este conflicto.

### Estilos específicos de página

Para añadir CSS exclusivo de una página sin contaminar el global, usar una clase en el `<body>`:

- `servicios.html` → `<body class="servicios-page">` → reglas en `pages/servicios.css`. Su cabecera (eyebrow + `<h1>` + subtítulo) va envuelta en `.servicios-hero`, que la centra y agranda el título (`clamp(32px, 5vw, 48px)`, el mismo rango que `.hero-title` de `index.html`) para darle el mismo peso visual que el hero del home y la cabecera de `ciudad.html` — a propósito **solo en `servicios.html`**: `alojamientos.html`/`viajes.html` comparten el mismo markup de cabecera (`.eyebrow`/`.section-title`/`.section-sub`, sin envolver) y se quedan con el tratamiento plano anterior, decisión explícita para no ampliar el alcance del cambio.
- `viajes.html` → `<body class="viajes-page">` — hoy sin CSS propio (`pages/viajes.css` se eliminó en la revisión de la familia de cards: solo tenía dos reglas huérfanas de `.event-*`); si hace falta CSS exclusivo, se recrea ese archivo y su `@import` en `styles.css`
- `ciudades-todas.html` → bloque `<style>` inline en el `<head>` (excepción deliberada: la mayoría de estilos de esa página son exclusivos suyos y no justifican clase de body — solo `.all-cities-hero` se comparte vía `pages/ciudades-todas.css`)

### Convenciones de componentes

- **Eyebrows de categoría** (`.eyebrow`): usar siempre `eyebrow--primary` (azul `#4648d4`) para categorías de contenido. `eyebrow--secondary` (rojo `#a93349`) queda reservado para destacar la marca o alertas.
- **CTAs de las cards de servicios** (`Card layout="service"`): externas → CTA `offer` (`.btn-primary-pill`); enlaces a una página interna → CTA `link` (texto + chevron, `to`). Ya no existe `.service-link` — se definen en `CTA_KINDS` (`Card.jsx`), ver [Familia de cards](#familia-de-cards-card).

### Zonas responsivas clave

Todo el CSS del proyecto es mobile-first: los `@media` usan siempre `min-width`, nunca `max-width` (verificado con grep sobre `src/css/` — no hay un solo `max-width` en un breakpoint global; `admin.css`, aparte, sí tiene los suyos propios). Los tres cortes reales, que coinciden con los tokens `--bp-sm`/`--bp-md`/`--bp-lg` de `tokens.css` (ver [Tokens del design system](#tokens-del-design-system-srcstyles) — los `@media` no pueden leer custom properties, así que estos valores están repetidos como literales en cada archivo):

- `@media (min-width: 600px)` (`--bp-sm`) — grids de 2 columnas (`pages/home.css` y otros)
- `@media (min-width: 900px)` (`--bp-md`) — desktop: `.city-page` max-width 1000px; el bottom-nav (`.app-bottom-nav`) se oculta y `body { padding-bottom: 0 }` (por debajo de este corte, mobile-first, `body` lleva `padding-bottom: calc(var(--nav-bottom-h) + 8px)` — 68px con el valor actual de `--nav-bottom-h`, pero como `calc()`, no como literal); el top nav/header (`.topnav-links`/`.icon-btn`/`.topbar-nav`) se hace visible
- `@media (min-width: 1200px)` (`--bp-lg`) — tier final de `.cta-card`/`.card-grid--service`

## Dependencias externas

Vía CDN, sin instalación:

- Leaflet 1.9.4 — `unpkg.com` (solo `mapa.html`)
- Supabase JS SDK (versión UMD) — `cdn.jsdelivr.net`, crea el objeto global `supabase` que usa `supabaseClient.js`
- Google Fonts — Syne + Inter
- Material Symbols Outlined — Google
- Nominatim — API pública de OpenStreetMap (geocodificación)
- CARTO Basemaps — tiles del mapa, con API key (ver [Tiles del mapa](#tiles-del-mapa))
- DeepL — solo desde la Edge Function `translate` de Supabase, nunca desde el navegador
- Unsplash — imágenes de países y ciudades

Instaladas vía npm (ver `package.json`):

- `vite` (v8) + `vite-plugin-static-copy` — build tool. El plugin copia tal cual `src/js/**`, `src/css/**` y `src/utils/**` a `dist/` porque los scripts clásicos (`<script src="/src/js/…">`) no pasan por el bundler de Vite; sin esos targets el build de producción devuelve 404 para ellos aunque `npm run dev` funcione
- `@vitejs/plugin-react` — transforma el JSX de `src/react/*` (ver [Navegación](#navegación) y [Familia de cards](#familia-de-cards-card))
- `react` + `react-dom` (v19) — para las islas de React del proyecto (ver el Stack en [Proyecto](#proyecto), arriba), no hay más React fuera de `src/react/`
- `@supabase/supabase-js` — cliente de Supabase (aunque en el navegador se usa la versión CDN cargada como `<script>`, no este paquete)
- `prettier` — formateo de código

## Convenciones de commits y ramas

- Commits con prefijo convencional: `feat:`, `fix:`, `refactor:`, `docs:`
- Ramas de feature: `feature/nombre-descriptivo`
- PRs hacia `main`. Se despliega en **Vercel** (proyecto `erasmus-verified`, enlazado a este repo de GitHub); los agentes de revisión asumen un _Vercel Preview_ por rama. El repo no contiene configuración de despliegue (`vercel.json`, `.github/`): no hay CI propia, la única comprobación automática que corre en el build es `check-sanitization.js` (dentro de `npm run build`).

## Agentes de revisión (`.claude/agents/`)

Siete subagentes de Claude Code, cada uno con un único cometido; `pr-orchestrator` los encadena antes de abrir una PR:

| Agente                     | Para qué                                                                                                                                            |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| `supabase-schema-guardian` | Verifica el estado **real** de la base de datos antes de escribir migraciones, tocar RLS o el admin — ya que el repo no tiene todas las migraciones |
| `functionality-reviewer`   | Revisión estática de lógica: tracking de CTAs, prioridad de ciudades, i18n completo, estados vacíos y de error                                      |
| `code-reviewer`            | Convenciones propias (tokens, `apply_migration` vs `execute_sql`, Conventional Commits, sin `alert()`)                                              |
| `security-auditor`         | RLS, XSS en el admin, URLs sin sanear en `href`, secretos expuestos, CHECK de URLs                                                                  |
| `design-reviewer`          | Consistencia con el design system (tokens, paleta por tema, tipografía, reutilización de `Card`); no revisa accesibilidad                           |
| `accessibility-auditor`    | Lighthouse, `prefers-reduced-motion`, contraste, touch targets ≥44px                                                                                |
| `pr-orchestrator`          | Ejecuta los anteriores en secuencia y consolida un único informe (la PR la abre Álvaro a mano)                                                      |

Además hay dos _skills_ invocables con `/`: `/add-city` y `/add-partner` (guían el alta por el panel de administración).

## Documentos y deuda técnica

Notas puntuales en `docs/` — son fotos de un momento, no documentación viva; ante una duda, manda el código y este archivo:

- `docs/page-boilerplate.md` — censo de qué `<link>`/`<script>` carga cada página pública y por qué. Útil como lista de comprobación al crear una página (dice "8 páginas"; hoy son 13 — no cubre `login.html`/`registro.html`/`recuperar.html`/`alojamiento.html`/`viaje.html`, creadas después).
- `docs/design-tokens-link-fix.md` — el bug de 4 páginas que no cargaban `tokens.css`/`typography.css` (las variables se descartaban en silencio).
- `docs/mobile-audit.md` — auditoría de los `@media` previa a los breakpoints 600/900/1200. Nombra clases que ya no existen (`.event-card`, `.collab-card`…): es histórica.
- `docs/tech-debt.md` — **categorías de partner en dos listas** que deben coincidir a mano: el `<select id="f-category">` del admin y `CATEGORY_META`. Hoy el admin ofrece 5 (`nightlife`, `housing`, `services`, `community`, `travel`) y `CATEGORY_META` tiene 20. (El documento dice que `CATEGORY_META` está en `map-helpers.js`: ahora está en `categoryMeta.js`.) **Corrección verificada contra la base de datos real (MCP de Supabase, 25 sept. 2026)**: el documento dice que las 15 categorías restantes "solo se pueden asignar tocando la base de datos" — eso ya no es cierto, si alguna vez lo fue. Hay un CHECK constraint real (`partners_category_check`) que limita `partners.category` a exactamente esas mismas 5 — un `INSERT` con cualquiera de las otras 15 (`restaurants`, `sports`, `culture`...) falla, ni siquiera por SQL directo. Las 15 categorías "de más" de `CATEGORY_META` no son alcanzables hoy por ningún camino, ni admin ni base de datos — para usarlas de verdad haría falta ampliar antes ese CHECK constraint.

**Deuda conocida que no está en ningún documento**:

- Los resultados de búsqueda de tipo **ciudad** no se registran (no hay partner al que atribuirlos y `cta_clicks.partner_id` es `NOT NULL`); persistirlos requeriría cambiar el esquema (p. ej. `partner_id` nullable + una columna `city_id`) y revisar las políticas.
- `initBottomNav()` en `index.js` es código muerto (busca `.bottom-nav-item`, la clase del bottom-nav antiguo; hoy es `AppShell.jsx`).
- `BOTTOM_NAV_ROUTES` de `speculationRules.js` no coincide con el bottom-nav actual (ver arriba).
- `fetchNightlifePartners()` (`partnersService.js`) no tiene ningún llamador; `.accordion-card-flag` (`home.css`) no tiene ningún uso.
- `README.md` describe la versión estática original y está obsoleto.
- Contraste y anillo de foco global débiles en Parties: ver [Familia de cards](#familia-de-cards-card) (reglas al ampliarla).
